# Design

## 特色教學頁、四校校園探索實景、義華家長分享（2026-09-26）

盤點兩個舊官網後，可以直接搬的內容都做了，細節與驗證見 README 同日段落。特色教學頁是第一版，使用者還沒比稿看過。

- **特色教學頁版型照 `/environment`**：hero＋三章索引 → 01 薄荷帶（四個年段卡）→ 02 米底置中（七個課程方向，四欄）→ 03 深綠舞台（五件事的拍立得，不翻面，桌機一排三張、交錯傾斜）→ 預約卡升起。
  - 品德培養沒有照片，做成橫跨兩格的深綠引言卡（「六歲定八十」），用 `grid-auto-flow: dense` 補位，兩排剛好排滿。
- **標題字型缺字的做法**：大標只用子集裡有的字（「四年八階段」「七大課程支柱」缺字，所以不當標題）。舊站課程名稱不能改，整組卡片標題改用 `--font-body`，同一個詞不混兩種字型。字型分支合併後可以改回標題字型。
- **內容來源一定要標**：義華站的課程特色寫明「照片與介紹取自義華校」；機構站的課程照校別不明，不標。
- **沒有佐證的宣傳語不搬**：「高雄獨家課程」「大推」拿掉，園方確認後再決定。
- **校園探索照片一定要 8:5**：`.tour-canvas` 是 8:5，`.tour-image` 用 `object-fit: fill`，比例不對會變形；熱點座標也是依 8:5 框的百分比。熱點文字只寫看得到的東西，問句導向參觀時問園方。
- **校別要有證據**才能掛到某校名下：檔名、所在頁面、畫面裡的校名。共用「校園環境」頁校別不明的照片不拿來當分校場景。有孩子姓名標籤的照片（例如崇德乾淨溫馨教室的收納櫃）不用。
- **義華的校園探索以後台為準**：正式站有 `campus_tour` 項目，改 fixture 不會生效。新場景寫成操作說明（`docs/website-admin/handoff-yihua-tour-20260926.md`），由園方在後台加。
- **內頁 hero 的 sizes 要寫實際顯示寬度**：手機版是固定高度的照片帶、`object-fit: cover`，橫幅照片會比視窗寬好幾倍，一律用 `pageHeroImage()`（`<img>` 與預載共用）。改了 `admission.css` 手機 hero 高度，要同步改 `PAGE_HERO_MOBILE_HEIGHT`（測試會擋）。
- **家長分享**：引言用影片標題裡家長的原話，海報用影片畫面（同活動影片規則），字卡上的名字與職稱裁掉。每校要有自己的影片才顯示，不拿義華代填。

## 手機活動影片改放義華 YouTube、各校 IG／YouTube 進後台（2026-09-25）

盤點舊官網後先做的兩項，細節與驗證見 README 同日段落。

- **活動影片第一支維持檔案影片**（`run`），後面接 YouTube。YouTube 不做自動預覽；全部換成 YouTube 的話，這一區進場時就完全不會動。
- **YouTube 海報一律用影片畫面，不用頻道封面**。義華頻道的封面是綠框、黃字加卡通圖示，撞到 PRODUCT.md 的反參考。做法是從 `i.ytimg.com/vi/<id>/maxres1–3.jpg`（影片 1/4、1/2、3/4 處的畫面，1280×720）挑一張，避開燒字幕和看得到名牌的畫面，縮成 720×405 WebP，再用 `youtube(網址, 標題, 海報)` 帶入。影片右上角的頻道浮水印不裁，因為播放時本來就會出現。
- **標題前面加校名**（「義華校：…」）。標題只給螢幕閱讀器用；活動影片放在機構首頁，要讀得出來是哪一校的活動。
- **社群帳號只填該校自己的**。目前只有義華有 IG（`ivy.kids.school.ig`）和 YouTube（`@IvyKidsVideos`），其他四校留「待提供」，不能拿義華的帳號代填。
- **IG／YouTube 由後台「五校介紹」管理**（`campus_profile` 的 `instagram`、`youtube`，預設空字串，跟 LINE 一樣走網址允許清單）。官網疊資料時，後台有這一欄就以後台為準，空字串代表「待提供」；9/25 以前存的版本沒有這一欄，才沿用 fixture（`content-overlay.ts`）。所以部署後義華的連結不用先動後台就會出現。**但部署後要到後台把義華的兩個網址填好並發布一次**：之後有人存義華的資料，表單會把空白欄位存成空字串，連結就不見了。
- **首頁五校卡社群列**：順序是 LINE、Facebook、Instagram、YouTube。IG／YouTube 有值才出現，沒有就不佔位、也不寫「待提供」；只有 LINE 會標「待園方提供」。桌機 1024／1440 排一行，手機 390 換成兩行。
- **換片判斷要用 `event.composedPath()`，不要用 `target.closest()`**。點擊的處理程式裡如果會換掉 DOM（例如播放鍵換成 iframe），冒泡到上層時 target 已經脫離 DOM。

## 常春藤環境頁 /environment、頁首只留分頁（2026-09-25）

舊官網「一日常春藤 Environment」本身只是四張入口卡，內容在四個分頁（幼兒保育、校園環境、一日流程、幼兒餐點）。搬進新站、版型照 `/admission`；mock 與比稿在 `design/environment-mockup-20260925/`（v1 四章 → v2 巧思比稿 → v3 只留今天吃什麼 → v4 定案）。

- **結構**：hero（`.photo-hero` 透明頁首）＋三章索引 → 01 幼兒保育（薄荷帶：五顆心引言、2:1 照片、五張照顧卡）→ 02 校園環境（淡米底置中標題：安全歡樂遊樂區深綠大卡、六個空間 4:3、五校線稿連到各分校 `#environment`）→ 03 幼兒餐點（深綠舞台：營養餐點書封面＋按鈕、四張餐點時間卡）→ 預約卡的紙從深綠升起。`EnvironmentContent.vue` 同時掛 `admission.css` 與 `environment.css`，adm-* 不另抄一份。
- **一日流程不搬**：使用者看過 v1 後拿掉，首頁「孩子的一天」已涵蓋。拿掉後深綠舞台改給餐點，預約卡才有地方「升起」。
- **文案**：各項目是舊站原文，只修錯字與標點；新寫的只有 hero、三段大標與引言。照片只確定戶外廣場是明華校（舊站檔名），其餘不標校別。
- **菜單直接導向，不抄進網站**（使用者裁定）：封面與按鈕依台北日期開到營養餐點書（FlipBuilder）當月那一頁，`utils/meal-book.ts`；1–6 月第 10–15 頁、7–12 月第 17–22 頁（第 16 頁是文章），`#p=` 深連結已實測。書改版或頁數位移要重核頁碼。
- **頁首選單只留真正的分頁**（使用者裁定）：拿掉首頁錨點（關於常春藤、孩子的一天、五所校園、最新消息），現為「常春藤環境、入學資訊」；頁尾連結不變，加上常春藤環境。選單寫在 `web/server/data/site-fixture.json`，後台不管選單、發布內容也不會蓋掉。目前頁標 `aria-current`。6 項時 901–1000px、1101–1245px 會把預約鈕擠出視窗（實測），2 項在 901–1440px 全部正常。
- **頁首膠囊**：`/environment` 加進 `SiteHeader.vue` 的 `PILL_PAGES`，同入學資訊頁桌機也收膠囊。
- **看過未選，勿再主動提**（v2 比稿）：A 五個「心」各畫金色手寫線、B 保育五件事捲到時依序打勾、C 照片細節放大鏡、E 預約卡參觀清單。D「今天吃什麼」（抄當月菜單、依日期顯示當天菜色）改成上面的導向。
- **字型**：標題有 21 字不在現行 LINE Seed Bold 子集（・乾加均套少工廁房撫暖毒消淨緒衛衡衣貼顆馨），合併前會退回系統字。本分支不動字型，等 `feature/admin-gaps-fonts-20260925`（官方完整字型分片按需載入）合進 main 後補齊，避免兩邊同時改子集。
- **SEO**：`environmentSeo()` 固定標題描述、分享圖沿用首頁、兩層麵包屑；sitemap 與 llms.txt 加入 `/environment`。

## 首屏調亮：遮罩只墊在文字後面（2026-09-24）

使用者要求首屏調亮一點。延續 09-14「遮罩只保護文字」的原則，把 web/ 桌機左側滿高直欄改成「淡直欄＋文字後光暈」，更新 09-16 明亮版的遮罩數值。

- **三層**（`.studio-hero::before`，由上到下）：頂部導覽帶 `.6` 實心到 76px、140px 內淡出；橢圓光暈 `radial-gradient(40% 42% at 24% 49%, .68, .6 55%, 0)`，中心對齊 1024–1440 各寬度的文字塊中心（實測 22–24% × 48.5–48.8%）；左側直欄保留原形狀、強度降為原本三成（`.23 / .22 34% / .17 42% / .04 55% / 0 62%`）。手機只有頂部帶，同步改 76／140px。
- 遮罩色走 `.studio-hero` 上的 `--hero-scrim:18 29 24`（原本字面值的同一色），不改全域 `--ink`。
- 頁首收合的 `background-size` 必須逐層寫（`100% 0,auto,auto`）：清單比圖層少時會循環套用，光暈會被收成 0 高。之後增減圖層要同步改這行。
- **量測方式**：ffmpeg 抽桌機／手機影片各 12 幀＋封面，依 CSS 遮罩離線合成（與瀏覽器截圖平均差約 1/255），每行文字取範圍內最亮背景像素，不計文字陰影。門檻：內文／小標／導覽 ≥4.5、大標 ≥3.5。結果：最差為手機導覽 4.66、1280 小標 4.79、1280 內文 4.85；大標 3.49→5.76（最差值反而提高）。餘裕已薄，再調亮要先量，模型在 `output/hero-brighten-20260924/search.py`。
- 等比例把舊直欄調淡行不通：淡 10% 時 1280 大標 3.06、手機導覽 4.38 就不過，亮度只多約 1 L*。亮度要從沒有字的地方拿。
- 整體亮度（封面 L* 平均）：桌機 36.3→40.8、手機 45.9→47.7；左下角 26.5→約 42，頂部粉紅衣服恢復原色。影片本身不加 `filter`（會同時把文字底調亮，也會動到已確認的膚色）。

## 頁尾回到 A 深森林綠底＋文字全白（2026-09-24 晚上定案，取代同日的 R）

使用者看過上線的 R 後，在第三批 R 變化（`design/footer-colour-directions-20260924/` U–Z）之後決定「走 A」，再比文字顏色 `design/footer-text-colour-a-20260924/`（10 案），選 **1 全白**。Nuxt 共用 `SiteFooter.vue` 已套用，首頁、分校、預約（含 manage）與後台草稿預覽共用。

- 底色 A 深森林綠 `#24483F`；品牌中文、IVY KINDERGARTEN、導覽、標語、「我們的大家庭」、五校連結、版權**全部純白 `#FFFFFF`**，不再有暖金點綴與灰綠小字，層次只靠字級與字重。純白是使用者指定，不要改回帶色偏的米白。
- 版面回到 A 的單色：拿掉 R 的 `.footer-bar` 滿版底列，版權列上方恢復 1px 分隔線 `#526D61`；版權與備註都空時整條底列省略，行為不變。
- 對比：所有文字 10.13。鍵盤焦點外框改白色；強制色彩時底為 Canvas、字與分隔線 CanvasText、焦點 Highlight。
- 首頁最新消息區仍漸退成暖白 `--paper` 再接深綠頁尾，`HomeNewsTransition.vue` 不動。色票在 `web/app/assets/css/tokens.css` 第 6 節只留 `--ivy-footer-bg`／`--ivy-footer-text`（指到 `--ivy-white-pure`）／`--ivy-footer-line`，R 用的 muted／brand／accent／bar 變數已刪；文案、字型、欄位、斷點不變；凍結 vanilla 原型不回寫。
- 看過未選：文字 0 現行米白＋暖金、2 白字＋暖金、3 白字＋淡綠小字、4–7 米白＋薄荷／暖黃／杏桃／天藍點綴、8 奶油黃字、9 淡綠同色；底色第三批 U 薄荷、V 蜂蜜奶油、W 霧玫瑰（皆配深綠底列）、X 暖金底列、Y 核桃底列、Z 燕麥兩階。
- 修改前快照 `versions/before-footer-text-white-20260924-194142/`；驗證 `output/playwright/footer-text-white-20260924/`。

## 入學資訊頁 /admission 改首頁版型（2026-09-24）

使用者要求入學資訊頁的版面與風格靠近首頁；先出 mock（`design/admission-homestyle-mockup-20260924/`），再依 mock 改正式頁。下方「上線版」的結構、提醒條、hero 明體大標被這一版取代，內容歸屬、選單、分班規則不變。

- **結構**：滿版 hero＋章節索引 → 01 入學流程（首頁「關於常春藤」的薄荷色帶、巨大淡字、3:2 圓角照片；步驟用虛線路徑＋半透明卡，最後一步金色）→ 02 分班對照（五校區塊的淡米底、置中標題；班別分頁用大號年齡數字＋底線，深綠生日查詢與出生區間放同一張 24px 圓角卡，完整表收在「看完整對照表」膠囊）→ 03 新生準備（孩子的一天的深綠舞台、貼膠帶拍立得，正面勾必備品、翻面看叮嚀；下方每週穿著／接送／註冊三張半透明卡）→ 04 收退費（最新消息那張 28px 圓角紙從深綠舞台升起；補助用活動卡三色＋大數字，退費規定用消息清單細線＋圓形加號）→ 預約卡（照片＋金色膠囊鈕）。
- **順序**：分班對照從最後提到第二段，家長看完流程最先問讀哪一班。
- **頁首**：hero 加 `.photo-hero`，studio.css 首頁透明頁首的 21 條 `body:has(.studio-hero)` 改成 `body:has(:is(.studio-hero,.photo-hero))`；SiteHeader 的膠囊頁面加 `/admission`，桌機也收膠囊（其他內頁桌機仍不收）。
- **拿掉**：麵包屑（結構化資料照舊輸出）、黏在頁首下的分頁列（改 hero 章節索引）、金色左框提醒條（提醒改放 hero 動作鈕下方，金色圓點＋小字，有值才顯示）。
- **字型**：大標改回 LINE Seed 700，hero 小標改內文粗體（LINE Seed 子集沒有「訊」）。`noto-serif-tc-500-admission.woff` 已不再被使用，@font-face 仍在 typography.css，要清掉時一併刪檔。
- **hero 遮罩**：用實際照片像素量過（1440、1024 桌機、390 手機分開量，取文字框 95 百分位亮度）：內文 ≥ 5.9、頁首導覽 ≥ 6.9、手機漢堡鈕 6.7。1100px 以下文字區較寬，遮罩另設一組。

## 入學資訊頁 /admission（2026-09-24 上線版）

舊官網「常春藤入學」四個分頁（寶貝入學流程、新生入園須知、收退費辦法、分班表）合成一頁，版面沿用分校頁；mock-up 與舊站內容盤點在 `design/admission-mockup-20260924/`。

- **結構**：hero（明體大標）→ 分頁列（桌機黏在頁首下，900px 以下不黏，避免撞到浮動膠囊）→ 頁面提醒 → 入學流程（桌機橫向時間軸最多 6 欄、手機直向）→ 新生入學二部曲（必備品可勾選、不存）→ 每週穿著／接送／註冊三張卡 → 補助卡＋育兒津貼橫條 → 退費規定手風琴 → 分班對照（生日查詢＋本學年／下學年出生區間表）→ 預約橫幅。
- **內容歸屬**：區塊大標固定寫在頁面（標題字型子集）；金額、步驟、須知、退費規定由後台 `admission_content` 管理（super_admin）。分班規則是法定年齡，寫在 `web/app/utils/admission-classes.ts`，學年度用台北日期、8/1 換年。
- **頁面提醒**取代 mock-up 的「頁面示意」黃膠囊：金色左框的米色條，有值才顯示；園方確認金額後在後台清空。
- **選單**加第五項「入學資訊」（頁首、手機選單、頁尾）。901–1000px 選單間距 20→14px，否則預約鈕被擠出視窗。
- **字型**：LINE Seed Bold 由原始 ver02 補 12 字（二冊囉寶曲月楚註貝退遲部）重切，725→737 字、既有字寬逐字核對不變；明體另切 `noto-serif-tc-500-admission.woff`（8 字）給 hero 大標，避免 Windows 落到新細明體。
- 分享圖沿用首頁，不另產。

## 品牌規範與色票收整（2026-09-24）

- 官網顏色收成一組：`web/app/assets/css/tokens.css` 是唯一來源，`nuxt.config.ts` 第一個載入。原本 `styles.css` 與 `studio.css` 各有一份 `:root`，互相覆蓋 `--green`／`--paper`／`--cream`／`--leaf`（`studio.css` 生效），現在只留生效的那份。
- 全站品牌色沿用短名（`--green`、`--paper`…）；只屬於某區塊的色用 `--ivy-<區塊>-<用途>`。元件的區域變數（`--visit-ink`、`--footer-bg`…）留在原處，值改成引用色票，作用範圍不變。透明度一律用 RGB 三元組（`--ink`、`--on-dark`、`--ivy-forest-rgb`…）。
- 重複的值合併：分校資訊色票原本在 `CampusBoard.vue` 與 `visit-booking.css` 各寫一份，森林綠 `#203f32`／`#c5ded0`／`#e6f1e7` 在理念區與預約頁各寫一份，消息紙色 `#faf7ef` 兩份。
- 例外：`app.vue` 的 theme-color、開場布幕 3D 材質、hydration 前布幕底圖、拍立得 WebGL 備用色寫在 TypeScript，讀不到 CSS 變數。
- 後台維持 09-22 的獨立色票，只把校園探索熱點與素材庫縮圖上 8 處色碼收進 `--on-photo*`、`--photo-caption-*`。
- 品牌規範整理成 `docs/brand/brand-guidelines.md`，內容只取既有定案。
- **畫面不變**：同一個 API 與內容下，改前改後的正式建置在 1440／390px、8 個頁面、多個捲動位置、選單與 hover 狀態下，34,988 筆元素計算後的顏色屬性完全相同；26.5 萬筆 CSS 變數解析值中，只有頁首膠囊與首屏底線 4 個變數的寫法不同（舊版被壓縮器改寫成 `#ffffff47` 等 8 位元近似值，新版保留原本的 `.28`），32 張截圖逐像素比對 0 差異。

## 頁尾改用 R「燕麥＋深綠底列」（2026-09-24 定案，同日晚上已由上方「A 底＋文字全白」取代）

使用者要求探索深森林綠以外的頁尾顏色，比稿 `design/footer-colour-directions-20260924/`（兩批共 17 個新案＋現行 A），選定雙色的 R。Nuxt 共用 `SiteFooter.vue` 已套用，首頁、分校、預約（含 manage）與後台草稿預覽共用。

- **主體**淺燕麥 `#EFE8DA`；導覽與五校連結 `#27433A`、品牌中文 `#1F3A31`、英文品牌名與「我們的大家庭」暖褐 `#7A5A24`、標語 `#5B6358`。
- **版權列**滿版深森林綠 `#24483F`、字 `#D9E2D9`。底列多包一層 `.footer-bar` 滿版容器（內層仍是 `.container.footer-bottom`），原本的 1px 分隔線拿掉，由兩塊顏色的交界取代；版權與備註都空時整條底列省略，行為不變。
- 深綠只留在最後一條：頁尾整體變輕，也不再和五校底板卡、孩子的一天連續三段深綠。
- 最低對比：主體 5.11（標語）、底列 7.63。鍵盤焦點沿用點綴色（暖褐）；強制色彩時兩塊底都變 Canvas，版權列上方補回 1px CanvasText 分隔線。
- 首頁最新消息區仍漸退成暖白 `--paper` 再接燕麥頁尾（比稿時就是這樣看的），`HomeNewsTransition.vue` 不動。
- 色票仍為元件內 scoped tokens，不改全域 `--paper`／品牌色；文案、字型、欄位、斷點不變。凍結 vanilla 原型不回寫。
- 修改前快照 `versions/before-footer-colour-r-20260924-084905/`；驗證 `output/playwright/footer-colour-r-20260924/`。

## 常春藤的一天：背景影片畫質（2026-09-24）

- 全螢幕 `object-fit:cover` 背景保留現有最佳素材 1440×810；桌機直接交付 `day-film.mp4` 的位元相同副本，手機從同一素材以 CRF 25 輸出。不要再將手機版降成 480×270，也不要以已縮小的 `day-film-mobile.mp4` 二次轉碼。
- 剪輯、16:9 構圖、25 秒／24fps／600 幀、無音軌與 faststart 維持；遮罩與播放控制沿用既有設計。新檔使用新雜湊網址，保留舊資產供既有版本引用。
- 4K 原片目前不在記錄路徑；這次保留既有素材細節，不放大冒充 4K。手機檔約 7.49 MB，保留捲入才下載、減少動態／省流量／慢速連線時使用靜態封面。

## 關於→孩子的一天 接力：維持 09-18 原樣，不加接力動畫（2026-09-24 改回）

使用者 09-23 晚想讓訪客發現「關於常春藤」的「常春藤」接成「常春藤的一天」，比稿後在 09-24 上線了「關於」掉進句首再沉下去的版本（15e3c9d）。同日看過後要求**改回沒有接力動畫的樣子**。
- **規則：接力維持 09-18 seam=2 原樣。** 擦除線掃過浮水印「常春藤」時，同一個位置的字從淡綠變成白色大標；「的一天」在過字後段整組淡入並從左滑回 .14em（`--relay-day`）。不停頓、不加任何動畫。簾幕距離桌機 .85 屏、手機 .55 屏。
- **已試過並改回、勿再主動提**：
  - 停拍：擦除線在兩行之間停住吃掉一段捲動。
  - 過字放慢。
  - 「的一天」逐字接上。
  - 「關於」掉進句首湊成「關於常春藤的一天」，之後的五種消失方式（擦掉、沉下去、掉出去、擠出去、瞬間消失）。
  - 「關於」滑過去對齊、只讓「常春藤」浮出。
- 比稿紀錄在 `design/relay-discovery-mockup-20260923/`、`design/relay-drop-mockup-20260924/`。上線過的做法可用 `git show ba6a03e`（feature）／`15e3c9d`（main）取回。
- 金線、幽靈「關於」、浮水印淡出、文字提示本來就已否決。

## 開場布幕：舞台光、褶子與帷幔接點精修（2026-09-26）

使用者看了倒數「1」的截圖後要求全面優化。以下是新增規則，時序、片頭、流蘇都沒動。

- 帷幔綁點不能有硬的直線：垂幔的前後深度改用 `sin²`，在綁點處斜率是 0；下擺仍用 `sin`，保留尖角。綁點往後收約 0.018，做成一小段直褶。不要改回 `sin` 鼓起，法線會在綁點翻轉，變成一條貫穿帷幔的接縫。
- 舞台光往兩翼衰減：`wings = 1 − 0.3·smoothstep(0.35, 1, |x|/stageWidth)`，和上方 flies 的暗部相乘，布幕、帷幔、編繩共用。腳燈寬度 0.16 → 0.20，強度預設 0.8 → 1.0，下擺編繩維持 0.35。
- 絨布是正面暗、轉向的斜面亮：`velvetCore` 0.8 → 0.7，側光 0.3 → 0.36。纖維雜訊對比 0.06 → 0.04（大螢幕上像木紋）。
- 褶子間距加一道低頻漂移（每片各自的 seed），深淺也分段變化，避免兩片看起來等距、互為鏡像。
- 片頭片框邊緣收窄（0.93–1.04 → 0.965–1.015），外面只留一圈約 0.018 寬的窄光暈，不做大片粉紅光暈。
- 不改的部分：倒數期間整面布幕的亮度（平常狀態下投影已經夠亮），以及「略過動畫」的配色。使用者截圖裡按鈕和投影偏暗，是截到「1」結束後 240ms 的淡出途中；底部的深色細條在 Chrome 同比例（1466×690）下重現不出來。
- **30th 緞帶要平整（同日，使用者要求）**：緞帶改用和倒數片頭一樣的長焦投影（距離 12），人物校徽維持 3.2。兩種投影在 IVY KIDS 條內（`logoUv.y` 0.19–0.30）漸變銜接，不在 876–878 列的空隙硬切，否則兩段會錯位。緞帶上的褶子明暗只留一點（`crestFold` 0.92–1、受光 0.88–1），失焦量減到三成。這條更新了上方 A/17「校徽維持 3.2」的敘述：只有緞帶以上維持 3.2。
- 靜止時的布幕樣子變了，所以五張首屏海報已用 `render-posters.cjs` 重產並更新 `?v=`。

## 開場布幕：校徽停留拉長（2026-09-23 晚）

- 使用者要校徽出現久一點：`LOGO_DURATION` 1.5 → 2.5 秒，全亮從約 0.7 秒變成約 1.7 秒（光圈開 480ms、收 340ms 不變）；倒數 3 秒、拉幕 3.4 秒不變，總長 7.9 → 8.9 秒。本條取代下方各節「校徽 1.5 秒／總長 7.9 秒」的敘述。
- 時序只改 `entrance-timeline.ts` 的常數；引擎、看門狗（總長＋3.6 秒）都以毫秒換算，會自動跟著改。載入上限 2.8 秒、1.8 秒掛載門檻與總長無關，不用動。

## 手機內頁頁首與小字（2026-09-23 晚）

- 分校頁與預約頁在 900px 以下沿用首頁膠囊：捲過 40px 收合、選單用同一張選單卡；桌機內頁維持展開頁首。膠囊規則以 `body:has(:is(.studio-hero,.is-pill-nav))` 作用，**`:is()` 裡只能放單一 class**，否則特異度改變、首頁 cascade 會跟著變。
- 手機選單開著時鎖背景捲動（`html.menu-locked`），桌機選單卡不鎖。
- 預約頁本身（`/visit`、`/visit/<校>`）手機不放「預約參觀」鈕；查詢／取消頁保留。
- 要閱讀的中文內文在手機至少 14px（`--fs-sm`）；10–12px 只留給英文小標、圖說、版權列等短字。

## 首頁手機版：活動影片取代近期活動、最新消息上下排列（2026-09-23 D 定案）

使用者從 `design/news-carousel-mobile-20260923/` 選 D（第一輪 A 單張翻頁／B 中央聚焦／C 疊卡之後指定），只改 640px 以下；桌機維持近期活動＋C 原地換片。
- 近期活動（`.hn-events`）在手機隱藏，改放 `HomeFilms.vue`（`.hn-films`）：鼠尾草綠底 `#acbe9b` 滿版色帶（取自使用者附的參考截圖），吃掉消息區上方 44px／紙頁轉場時 88px 的留白；標題「活動影片」＋英文小標 CAMPUS FILMS。
- B 中央聚焦環形輪播：當前影片 80% 寬置中，左右露出前後一支（縮 0.9、淡 0.3），無限循環；滑動（水平意圖才接手）、點兩側露出的影片、點圓點、左右鍵都能換。圓點照參考圖：10px 同尺寸、點擊範圍 36×44，當前實心白、其他白 22%。**圓點下方不放文字**（使用者 21:45 要求拿掉），標題只當螢幕閱讀器名稱。
- 影片兩種可混放（`utils/campusFilms.ts`）：檔案＝只有當前那支設 `src` 靜音循環預覽（區塊可見 60% 以上、分頁在前景、`mayAutoplay` 通過才播），右下暫停鍵；YouTube＝縮圖＋播放鍵，點了才插 `youtube-nocookie` iframe 有聲播放，換走即移除。YouTube 不做自動預覽（播放器很重、開頭蓋標題與推薦）。比例 16:9。播放 YouTube 時在影片上滑不會換片，要點兩側或圓點。
- 第一版影片是官網既有素材剪段：`hero-mobile`（首屏已載入）整段、`/assets/day-film-mobile.mp4`（960×540 舞台表演原檔，滑到第 2 支才下載；最佳化的 480×270 版在置中卡片裡會糊）切 0.2–5.4／7.6–12.2／14–19.4 秒。海報 `campus-film-*.webp` 用 CSS 背景（桌機 display:none 時不下載；`<video poster>` 會）。後台還沒有影片欄位，換正式影片或 YouTube 連結改 `campusFilms.ts`（`youtube('<網址>', 標題)`）。
- 最新消息在手機改成縮圖列表：108px 4:3 縮圖＋分校日期＋標題，整列可點，列間分隔線 `--hn-rule`（霧藍底上原本的 `#d9ded3` 看不到）。桌機三欄卡與 C 原地換片不變。
- 減少動態：不自動播放（按播放鍵才播）、換片直接切換。

## 最新消息：C「原地換片」自動輪播（2026-09-23 定案）

使用者要最新消息有輪播、不要按鈕，從 `design/news-carousel-20260923/` 三版（A 緩慢漂移、B 逐張推進、C 原地換片）選 **C**，取代 09-16「不使用自動輪播」。只改 `web/`（`NewsDialog.vue`、`composables/useNewsRotation.ts`、`utils/newsRotation.ts`、`studio.css`），凍結原型不回寫。A、B 落選。

- 卡片位置不動，一次三則；超過三則才輪播，每 6 秒換下一組（fixture 6 則＝兩組交替），最後一組不足三則從頭補滿。近期活動不動。
- 換組：三格由左到右間隔 160ms；新照片由上往下刷開（同近期活動卡 hover 刷色方向）＋ 1.08→1，舊照片 1→1.03 並壓暗；舊字 80–420ms 上移淡出，新字 460ms 起上浮，兩行不重疊。換組時每格暫時疊新舊兩層，舊層 `inert`，動畫完收成一層。
- 標題列「所有最新消息」左側是被動倒數「01 / 02」＋ 72px 細線（`aria-hidden`、不是控制項），暫停時變灰；不輪播時 `visibility:hidden` 不跳版，640px 以下不佔位。
- 暫停：滑鼠停在消息卡上、鍵盤焦點（`:focus-visible`）在卡內、消息對話框開著、卡片可見不到 35%、分頁在背景。計時沿用分校的 `createCarouselClock`（只算實際觀看時間）。減少動態不播；640px 以下維持原生橫向捲動、不輪播（手機輪播另案）。
- 焦點：滑鼠關掉對話框後焦點回到舊標題，舊層一設 `inert` Chrome 就把焦點丟到 body，所以**推入新層前先記下焦點、新層出現就交給新標題**（不能等動畫結束）。
- 未定：WCAG 2.2.2 要求的常駐暫停方式。使用者要求不要按鈕，目前只有上述暫停條件。
- 修改前快照：`versions/before-news-carousel-c-20260923-205644/`。

## 手機首訪載入：布幕資源先到、首屏影片讓路（2026-09-23 晚）

- 開場布幕的投影貼圖與倒數字型由 `entranceBootstrap` 在 `DOMContentLoaded` 預載，`crossOrigin=anonymous` 要與 three `ImageLoader`／`FontFace` 一致；DCL 晚於 1.5 秒就不預載（布幕 1.8 秒後才掛載會直接放棄）。不要改回「等引擎建好才抓」，一般 4G 會永遠趕不上 2.8 秒上限。
- 投影貼圖只能用**無損**格式；有損 WebP 會讓白紙區出現去背斑點。瘦身靠把去背後全透明的白紙像素壓成純白（輸出逐位元不變），換圖時用 `scripts/optimize-entrance-projection.py` 驗證後再寫檔。
- 布幕還在 pending 時首屏影片不載；布幕開演或放棄後才開始。
- 手機首屏影片用 CRF 26（VMAF 手機模型 99.88）；桌機母帶維持 CRF 18。
- 首頁仍 prefetch three.js 的 vendor chunk：`nuxt.config.ts` 的排除規則只比到 `entranceCurtain.ts` 本身，three 被拆成另一支 chunk。這支 prefetch 讓布幕就緒更早，**不要當成 bug 修掉**。

## 開場布幕質感精修（2026-09-23 A/16）

延續 A/15，以下是新增規則。比稿結果（2026-09-23 A/19 使用者拍板）：深紅光澤與白色片頭燈都採用並改為預設，`?sheen`／`?lamp` 參數已移除；校徽與緞帶維持暖金燈色。

- 帷幔不進陰影圖（VSM 會把 receiver 也畫進去）。它在布幕上的陰影由布幕著色器依帷幔下緣自己算，是柔和的接觸陰影，不做銳利投影。
- 縫在布上的編繩要在頂點上前推（約 3mm），不靠 polygonOffset；否則同曲面會 z-fighting，布色從編繩透出。
- 絨布纖維紋理要以螢幕尺寸定密度（約 2 CSS px），並用 `fwidth` 在收攏時淡出，避免開幕時閃爍。
- 兩片前緣要收平，前片前緣加分幕陰影；投影跨過中央時的錯位應藏在分幕線裡，不能讓圓圈斷開。
- 片頭的老片效果（顆粒、閃爍、灰塵、毛髮、刮痕）只能在片框內；閃爍最多約 10%，不做整個畫面的明滅。
- 帷幔流蘇每個連接點一顆，數量跟隨畫面比例；只在開幕後擺動，倒數期間靜止。
- **流蘇是金色絲線，不是金屬（2026-09-23 A/20，使用者嫌「金屬吊飾」）**：`MeshPhysicalMaterial` metalness 0、sheen 取代鏡面高光；裙擺每根線各自深淺、各自長短（下緣參差、只畫外側所以縫隙透出絨布），領口下有線束陰影，頭部是水平繞線的圓球。不要回到有縱向凹槽的實心錐體＋金屬度（會被看成黃銅子彈）。尺寸 1.1 倍（原 1.35 太搶、離場時拖在頁首字上）。
- **「略過動畫」在拉幕開始時退場（2026-09-23，使用者說太晚消失）**：倒數結束、片頭燈熄、布幕開始拉開的同一刻（`opening > 0`）淡出 240ms，之後 `visibility:hidden` 不可點、不可聚焦；Escape 仍可略過。原本留到整段 7.9 秒結束，拉幕後會疊在頁首「預約參觀」上、像網站自己的按鈕。
- 帷幔飛出距離是 `VALANCE_FLY = 0.5`（原 0.4），三處共用（帷幔變形、布幕上的帷幔陰影、流蘇位置）；0.4 時 smoothstep 的減速尾巴剛好在畫面上緣，流蘇最後慢慢爬過 Logo 與選單文字。
- 對開布幕走軌道式：只做水平平移（可有下襬擺盪），下襬不得在拉幕時上揚露出底下頁面；提拉式的上收動作已移除（2026-09-23 A/18，使用者要求）。
- 片頭用長焦距投影（倒數專用、距離 12，校徽維持 3.2），圓圈在褶子上要保持近乎正圓；布的立體感改靠受光與褶子明暗，不靠形狀扭曲（2026-09-23 A/17，使用者要求）。

## 開場布幕：經典片頭倒數、帷幔定案（2026-09-23 A/15）

使用者確認帷幔與金底深字都採用，倒數改成經典電影片頭。本節取代下方 A/14 的「單一暖金細圈、LINE Seed 數字」和「帷幔只是比稿參數」兩條；A/14 的絨布打光、光圈校徽與投影原則照舊。

- 倒數是投影在絨布上的 Academy 式片頭：圓形片框亮、中央最亮；雙圈、十字線、掃針和數字是擋光的底片，所以數字是沒被照亮的深紅絨布，不是亮字。順時針掃過的一側較深，每個數字重掃一圈。
- 老片質感只落在片框內：片門晃動約 0.35%、12 fps 閃爍 ±5%、每格 3 顆灰塵點、偶發刮痕。不做整個畫面的明滅。
- 片框在校徽光圈收成一點的地方重新打開（260ms），接著是 3、2、1 的對焦拍點；「1」結束前燈光一亮後熄滅。
- 數字用 Oswald 700（只含 0–9 的子集），依墨色高度自動定字級；品牌標題字 LINE Seed 不用在倒數。
- 帷幔固定出現，開幕進度過半後升起離場；`?valance` 參數已移除。

## 開場布幕：絨布打光、光圈校徽、對焦倒數（2026-09-23 預覽 A/14）

依評析三塊一起改。本節取代下方「彩色校徽與圓框電影投影」中的掃針、扇形、光暈，以及「900px 以下倒數沿用原有圓環」；也取代「週年緞帶恢復金色」的遮光密度（負片）做法，緞帶仍是暖金。

- 絨布靠光成立：底色 `#561326`、sheen 1／粗糙度 0.55，外加依法線的摺肩亮邊與垂直絨毛條紋；上方 40% 漸暗，下緣暖色腳燈。褶形為圓鼓布面加窄深褶溝，振幅由上往下 0.78→1.18 倍；前緣收平到 45%，減少中央重疊處的投影錯位。
- 投影是光，不是顏料：聚光範圍乘以布色（亮紅而非米色盤），墨線只擋投影機的光，不把布壓得比周圍更黑；彩色區保留原色並帶 12% 布色。人物與緞帶用同一套算法，緞帶依亮度投成暖金（金底深字）。
- 校徽由柔邊追蹤光圈帶進帶出（480ms 開、340ms 收成一點），接著才出倒數。
- 倒數桌機、手機同一樣式：單一暖金細圈、LINE Seed TW ExtraBold 數字、圈內保留酒紅絨布。每個數字落下時有約 100ms 的失焦加微亮拍點；「1」結束前燈光一亮，隨退光熄滅。
- 帷幔 `?valance=1` 只是比稿參數，定案後要從預覽移除或併入預設。
- 陰影地雷：three r186 的 VSM 會把 `receiveShadow` 物件也畫進深度圖；有頂點變形的網格，就算只接收陰影也要掛變形用的 `customDepthMaterial`，否則會以原始平面遮光。

## 關於常春藤：單張圓角照片（2026-09-23 定案）

依使用者指定改成一張照片，框型比照參考截圖。

- 只顯示 `about.photos[0]`，3:2、16px 圓角，無米白框也無陰影，規格同 `.studio-intro-photo`。圖說放在照片下方靠右。
- 照片同日換成使用者提供的 `about-together`（長輩被孩子們圍住大笑）：母檔預先裁成 3:2（1204×803），所以不設 `object-position`；換照片時同樣先裁好 3:2 再跑 `scripts/optimize-site-images.py --only <代號>`。
- 桌機照片佔滿右欄寬度；≤900px 單欄時最寬 570px 並置中。
- **2026-09-23 下午 照片頂緣對齊標題字面頂**（取代原本「與文字欄頂端對齊」）：SINCE 小標獨立在上方當引子，照片與標題同一條頂線。≥901px 時 `.belief-main` 用 `grid-template-rows:subgrid` 把 SINCE／標題／內文拆成父層三列，照片跨標題＋內文兩列靠上，再加 `margin-top:calc(var(--belief-title-size) * .25)`（標題行高 1.5 的半行距）對齊字面頂而非行框頂。標題字級統一走 `typography.css` 的 `--belief-title-size`，改字級或行高要一起看這個係數；手機單欄不受影響。
  - 理由：照片固定 3:2，在常用寬度 1440–1920 高度約 404–437px，恰好接近「標題字面頂到內文末行」的 430–455px，所以頂對標題時照片底緣落在內文末行附近、圖說與末行約同高，兩欄成一個方塊。1280 以下內文換行變多，左欄會比照片長 70–200px，屬正常的圖靠上、文字續排。
  - 否決：同日先試「照片對內文第一行」，照片比內文長出約 110px＋圖說、標題右側空一塊，頭輕腳重。「照片撐滿標題到內文」會隨寬度變比例（1024 寬變直幅），不採用。
- 取代下方「介紹區雙照片重疊維持原設計」「手機仍保留錯落雙照片」，以及手機背景段落裡的「雙照片構圖」描述；手機尾留白公式不變。

## 人物校徽置中基準（2026-09-23 修正預覽）

使用者回報上一輪 Logo 仍未置中；前次量測對象是包含週年緞帶的整組外框，不能代表人物校徽本身置中。本輪先以截圖中的皇冠、人物、月桂、IVY KIDS 為中心範圍：原圖 y=114–876，中心為 y=495。投影的垂直 UV 中心改為 583/964，金色週年緞帶自然接在下方，倒數圓框保持畫面中心。

此項只改置中參考，彩色人物質感、暖金緞帶、比例及 7.9 秒流程保留；取代下方「整組外框與畫面中心偏差 2px」作為人物置中證據的描述。新版預覽 A/13，證據 `output/playwright/crest-centering-20260923/`，快照 `versions/before-crest-centering-20260923-122208/`。尚未部署。

## 人物投影細修、共同置中（2026-09-23 最新定案）

人物、皇冠、月桂與 IVY KIDS 繼續彩色，30th Anniversary 緞帶維持暖金。依使用者截圖要求，清除人物淡色殘邊、加強布面投影感並置中；不重畫人物或修改原始 PNG。

- 米白底改在原始解析度先去除，輪廓局部去除紙底混色，再生成 premultiplied linear colour mipmaps；奶油色臉部保留。彩色投影使用衍生的記憶體紋理，金色緞帶仍讀原圖，兩者一併釋放。
- 人物保持清晰核心，柔光半徑隨布面深度變化；加上低對比顆粒與較深褶皺的光線衰減，適度保留布料染色及紋理。
- 取消原本上移 0.04 世界單位的設定，校徽、倒數與聚光共用 (0,0) 中心；原比例及 1.5＋3＋3.4 秒流程保留。
- 高密度畫面最高 2× DPR、450 萬繪圖像素，resize 同步重算。此為渲染精細度調整，不宣稱重建原圖細節或物理準確的光學模擬。
- 快照 `versions/before-refined-projection-20260923-120523/`，證據 `output/playwright/refined-projection-20260923/`，獨立建置 `output/refined-projection-build/`；本機 3136，未部署。

## 下方週年緞帶恢復金色（2026-09-23 最新定案）

下方「30th Anniversary」整個緞帶區域改回彩色校徽之前的暖金投影，使用相同遮光密度、柔光與亮度；上方人物、皇冠、月桂與 IVY KIDS 維持彩色。shader 以原圖第 876–878 列的空白間隙切分，避免人物或上方品牌條被染金。原始圖片、比例、圓框倒數與 7.9 秒流程保留。此條取代下方彩色校徽段落中週年緞帶保留綠金原色的設定；桌機與手機同步，未部署。

## 選單五校 hover 切換（2026-09-23）

依使用者截圖要求，頁首選單的五校列在滑鼠移入或鍵盤聚焦時，同步切換下方校區電話、撥號連結與四平台社群；目前校區沿用原 hover 底色，移出後保留選擇，不搶走鍵盤焦點。觸控 pointerenter 不觸發滑鼠預覽，校名點擊／手機點選仍前往分校內頁。

初始校區仍由頁首電話比對已發布校區；互動後改用選中的已發布校區，此規則取代較早「分校聯絡始終以頁首電話對應校區為準」的限制。缺電話或社群保留待提供，不借用其他分校或機構帳號。中性乳白毛玻璃、字型、五校單列與機構社群維持既有定案。

已部署正式 web，deployment `e5ea1c35-ce8f-4b2b-9705-317294f76830`、release `01bdec18…`；五校 hover／鍵盤與 390px 觸控導頁皆經正式站驗證。證據 `output/railway-menu-hover-20260923-115855/`，Safari／iOS 實機未驗證。

## 彩色校徽與圓框電影投影（2026-09-23 最新定案）

使用者先要求 321 加回圓框、強化投影在布幕上的電影感，再要求人物 Logo 維持原本彩色。本節取代下方純數字預覽，以及早期將校徽統一染為暖金的設定。

- 桌機倒數採單一暖金細圓框，保留數字光學置中與原比例；加上低對比順時針掃針、細顆粒、局部燈光呼吸與深褶失焦。圈內維持酒紅絨布，無米褐片底與滿版齒孔。
- 校徽直接使用原 PNG 的 RGB：shader 移除近白紙底、清理抗鋸齒白邊，保留奶油色臉部與完整品牌色。局部降低紅布反射的染色，讓藍衣、粉紅裙、綠金緞帶清楚，同時保留布面法線造成的明暗及形變；原始圖片不改寫。
- 校徽彩色適用桌機與手機；900px 以下倒數沿用原有圓環。校徽與倒數仍共用中心／高度，1.5＋3＋3.4 秒流程及所有略過／備援不變。
- 修改前快照 `versions/before-cinema-projection-20260923-114356/`、`versions/before-colour-logo-20260923-114736/`；證據 `output/playwright/cinema-projection-20260923/`；獨立建置 `output/cinema-projection-build/`，本機 3136。未部署。

## 純數字倒數預覽（2026-09-23）

使用者要求查看「只留數字」的畫面，電腦版先改為紅布幕中央的暖金 3、2、1，移除膠卷片底、圓圈、十字線、掃針、齒孔與底片磨損。此為本機比較預覽，尚未指定正式上線。

- 保留上一版數字比例與光學置中；由布幕 shader 直接投射數字，光影隨布褶變化，無額外矩形平面。舊膠卷 renderer 已從執行路徑移除，完整版本留在快照。
- 校徽先行、各數字完整一秒、最後拉幕的總長仍為 7.9 秒；900px 以下保留既有暖金圓環投影。Logo 原圖、略過與備援皆維持。
- 快照 `versions/before-number-only-20260923-112736/`；證據 `output/playwright/number-only-20260923/`；獨立建置 `output/number-only-build/`，本機 3136。未部署。

## 電腦膠卷改為中央橫框與數字光學置中（2026-09-23 最新定案）

依使用者要求取消滿版膠卷，改為中央 16:9 橫框，四周保留紅布幕。寬度取容器 64%、880px、容器高度 1.2 倍的最小值；圓框直徑為框高 76%，校徽同步採相同高度與中心，避免前後比例跳動。

- 移除膠卷內原本偏上的座標偏移，膠卷、圓圈與十字線使用同一中心。數字橫向依實際墨色重心置中，垂直依字形可見上下界置中，修正尤其明顯的「1」偏右感。
- 保留片邊齒孔、掃針與低對比底片磨損；900px 以下的暖金布面投影、校徽先行／321／拉幕時序與備援不變。此節取代上一版電腦滿幅膠卷。
- 快照 `versions/before-film-panel-20260923-111907/`；證據 `output/playwright/film-panel-20260923/`；獨立建置 `output/film-panel-build/`，本機 3136。未部署。

## 電腦版採復古膠卷倒數（2026-09-23 最新定案）

依使用者提供的老電影片頭參考圖，900px 以上的電腦版倒數改成滿幅米褐色膠卷，兩側深褐片邊與齒孔、深褐大數字、單圈粗圓框、十字定位線與每秒旋轉的掃針；細顆粒、污點與刮痕保持低對比，不使用整畫面明滅。

- 仍先在紅布幕投影校徽 1.5 秒；倒數時由獨立 Three.js 平面呈現平整膠卷，3、2、1 各一秒，接著 180ms 淡回布幕，再沿用 3.4 秒拉幕。圓框與校徽共用中心與高度，總長 7.9 秒不變。
- 900px 以下維持既有暖金雙圓環布面投影，切換視窗尺寸即更新；原始 Logo、無障礙宣告、略過與備援機制不變。新增的平面與材質一併釋放。
- 此節只取代上一版電腦倒數外觀；修改前快照 `versions/before-desktop-film-20260923-094848/`，證據 `output/playwright/desktop-film-20260923/`。獨立建置 `output/desktop-film-build/`，本機預覽仍為 3136，未部署。

## 校徽先行與復古電影倒數（2026-09-23 最新定案）

依使用者要求，開場改為校徽先亮起 1.5 秒，再由 3、2、1 接替；各數字完整一秒，最後以 3.4 秒拉開布幕，總長 7.9 秒。此條取代下方歷史版本的「校徽與小倒數同時上下排列」及 6.4 秒時序。

- 校徽與電影倒數使用同一中心與投影框：圓盤外徑對齊校徽高度，維持校徽原始長寬比；手機共同縮放，切換不位移、不縮放。原始校徽 PNG 保留。
- 校徽在最後 220ms 退光，電影倒數接棒。暖金電影倒數採深酒紅大數字、雙圓環、細十字線、每秒順時針一圈的掃針與低對比底片顆粒；仍投射在 Three.js 布面，隨褶皺變形與明暗起伏。
- 最後的「1」完整顯示後，倒數圓盤用 180ms 退光，在布料開始移動前消失。沿用先前的厚絨、五次緩動、下襬延遲、陰影淡出與資源釋放。
- 無障礙狀態依序宣告週年、倒數、開幕；完整動畫 watchdog 隨新增校徽段延長至 11.5 秒，初始化上限仍為約 2.8 秒。保留略過、Escape、減少動態及所有既有備援。
- 修改前快照 `versions/before-film-countdown-20260923-094014/`；證據 `output/playwright/film-countdown-20260923/`。獨立本機 Nuxt 預覽在 3136，未部署。

## 開場動態與渲染精修（2026-09-23）

沿用前輪酒紅、柔金、校徽與倒數的比例，進一步精修播放細節。

- 開幕軌道改五次緩動，起步和收尾的速度、加速度均平滑歸零；下襬相對上緣的延遲略增，布料擺動頻率降低。完整 3 秒倒數＋3.4 秒開幕不變。
- 最後的「1」在第三秒結束後用 180ms 平滑退光，再接既有校徽退光與拉幕，畫面不再直接跳掉數字；讀屏時序仍於第三秒轉為開幕。
- 倒數時幾何和燈光固定，重用 VSM 陰影；開始拉幕、拖回前段或改變尺寸時重新計算。瀏覽器量測同一閉幕拖曳操作的 draw calls 24→10（兩次渲染合計），減少約 58%；這是繪製呼叫數，不是 FPS 或電量量測。
- 修正開幕尾段的殘留陰影：原本中央 canvas alpha 持續為 46/255（約 18%），關閉動畫才突然清除；陰影現隨拉幕淡出，在開幕進度 80% 時已完全透明。
- 預覽重播立即恢復首格；完成後隱藏「略過動畫」，避免疊住已揭開的首頁。原本一次播放／略過／減少動態／失敗放行機制保留。
- 快照 `versions/before-entrance-motion-20260923-092615/`，證據 `output/playwright/entrance-motion-20260923/`。本機整合，未部署。

## 桌機關於：段落離開大字才接力（2026-09-23 下午）

- **規則：動態版不分寬度，簾幕開始擦之前，內文與照片都要整段離開兩行浮水印。** 使用者要「關於常春藤」在捲動時出現在段落下方，不要壓在段落後面。下方 ≤900px 的「半屏＋1.52em」尾留白改成所有寬度共用；桌機另把 `.belief-layout` 的滿屏 min-height 設為 0，最後一行到「關於」上緣的間隔就等於 layout 下內距（48–78px）。
- 這個規則不能靠照片高度撐：舊雙照片版剛好成立，是因為照片比內文高；換單張 3:2 後就失效了。之後改關於的照片或文案長度，不用再調尾留白。
- 減少動態／無 JS 維持 .15em 的較短尾留白（段落落在「關於」下緣）。

## 手機關於背景與大字接力（2026-09-23）

依使用者截圖修正 Nuxt 首頁手機「關於常春藤」背景偏短：雙照片改為上下排列後，原 30% 閱讀視窗的尾留白會讓照片蓋住浮水印，簾幕掃到照片而非大字。

- ≤900px 單欄照片版的動態尾留白改為半個穩定閱讀視窗＋1.52 倍大字字級，對齊「關於」上緣；沿用 layout 原有 40／48px 留白，照片先離開兩行大字，再開始擦除。涵蓋手機橫放與平板窄版，保留既有簾幕距離（≤760px 為 55%）、首張拍立得間距、雙照片構圖及完整介紹展開。此條取代下方 2026-09-21 動態版 30svh 尾留白。
- ≤760px 浮水印與日常標題共用字級，六字寬度不得超出螢幕左右各 20px 的範圍，修正 320px 裁切與水平錯位。日常中英文副標以完整片語排列，放大文字時可分行。
- 手機浮水印 .28 規則移至 seam=2 共用規則之後，避免被 .5 蓋回；減少動態及無 JavaScript 保留原本較短尾留白，不增加轉場所需空間。首次繪製即採相同 CSS 幾何，延續網址列高度變動時的閱讀穩定性。
- 修改前快照 `versions/before-about-mobile-20260923-092601/`；前後截圖與驗證腳本 `output/playwright/about-mobile-20260923/`。只改 Nuxt 樣式，桌機、凍結原型與資料內容沿用。 已部署至正式 web，deployment `2bb67300-f755-4ef7-8787-ac02a14ef4a4`、release `8a3dd98d…`；正式站驗證在 `output/railway-about-mobile-20260923-093354/`。

## 開場投影的協調性精修（2026-09-23）

依使用者要求繼續檢視協調性，沿用 A、原校徽投影及完整 321 倒數。將布料調為較沉穩的酒紅，摺數減少、次級摺痕柔化，金邊降低金屬反光；中央保留暖金光、兩側光照略降。

- 校徽在桌機由 0.95 調為 0.86 世界座標寬度；手機可用寬度係數 1.52→1.28，約縮小 16%。校徽和倒數視為同一組，依整組高度做光學置中，縮短彼此間距。
- 倒數由 Georgia 改為系統圓潤粗體，與校徽「30」字形接近；數字投影框縮小，投影強度略低於校徽，保持主次。
- 投影淡入改為平滑 360ms，拉幕時約前 816ms 退光，減少校徽分裂的突兀感；數字每秒只做輕微亮度呼吸。完整 3 秒倒數＋3.4 秒開幕及所有略過／備援行為不變。
- CSS 初次繪製封面與略過按鈕同步配色。精修前後實際首頁截圖在 `design/entrance-curtain-a-velvet-20260922/harmony/`；本機快照 `versions/before-entrance-harmony-20260923-081253/`。此為本機精修，未部署。

## 30 週年校徽投影與 3、2、1 倒數（2026-09-23 追加定案）

沿用 A 左右對開，依使用者提供的 `04-v4-30th-Anniversary圓潤字體版.png`，把原校徽轉為暖金色投影光。原始 PNG 完整複製至 `web/public/assets/ivy-30th-anniversary-projection.png`，不重畫人物、皇冠、緞帶或字樣；此授權僅用於開場布幕，頁首品牌維持原樣。

- 投影直接加在 Three.js 布面材質的光照，依變形後的布面位置、投射角度與法線取樣；光線隨摺痕起伏，有柔和邊緣與聚光範圍。手機依可用寬度縮放，保留完整校徽及倒數。
- 流程改為每秒一個數字的 **3 → 2 → 1**，布幕在完整三秒內關閉，再接 3.4 秒對開；總動畫 6.4 秒。數字同樣投影於布面，開幕時校徽逐漸退光，不增加音效。
- 圖片與 renderer 就緒才開始倒數，載入時間不消耗或壓縮數字。沿用 1.8 秒的過晚 hydration 跳過，模組／圖片初始化等待至多約 2.8 秒；整體 watchdog 10 秒。無 hydration 時的 CSS 首屏封面仍於 5.5 秒自動放行。
- 保留同分頁工作階段一次、略過／Escape、減少動態、高對比、低速／省流量、背景切換、WebGL／圖片失敗的放行與資源釋放。`entrance-timeline.ts` 共用 production／預覽時序；預覽仍在 `design/entrance-curtain-a-velvet-20260922/`。
- 此節覆蓋下方舊版的開場文字、3.4 秒總時長及壓縮動畫策略；已整合本機 Nuxt，未部署。

## 首次進站布幕採 A 經典對開（2026-09-22 定案）

使用者選定三案中的 A，並要求更逼真的布幕。Nuxt 首頁採左右對開的紅色厚絨布，保留金色細邊與開場文案；B 升幕、C 柔弧只保留於原比稿紀錄。

- 以不規則垂褶、細部張力皺紋、程序化纖維貼圖、絨面物理材質與布料自身陰影增加厚度與質感；軌道先動、下襬稍後跟上。標準 3.4 秒，不增加音效。
- 同分頁工作階段僅播放一次；預設只攔初次首頁直接進站。減少動態、高對比、節省資料、慢網路、錨點、其他頁與已播放者直接顯示內容。保留略過與 Escape，結束後移除 modal、解除捲動鎖並釋放 WebGL 資源。
- 首屏封面不依賴 hydration 完成；module 載入、WebGL 失敗與過慢初始化均有直接露出首頁的路徑。緩慢初始化後依剩餘等待預算縮短動畫，總保護時間約 5.5 秒。
- 實作 `EntranceCurtain.vue`、`entranceCurtain.ts`、`entrance-policy.ts`，只由首頁接入；凍結原型不回寫。材質可重播預覽在 `design/entrance-curtain-a-velvet-20260922/`。此輪為本機整合，未部署。

## 手機保留桌機構圖（2026-09-22 定案）

使用者確認手機適配建議並要求實作，本節取代手機消息小縮圖直列、活動三張直列、分校近方形照片及拍立得 92% 寬度規格；桌機與其他已確認設計沿用。

- 640px 以下消息維持大圖／下方校區、日期與標題，圖片 3:2；消息及近期活動改為原生橫向捲動，單卡寬度為可用內容寬度減 36px、間距 16px，保留下一張的一角與原有活動三色。採 proximity scroll snap，單張時使用全寬。標題、所有內容入口、對話框與 DOM 順序保留，Tab 可依序顯示各卡；捲動區留 8px 內距容納焦點外框。
- 700px 以下分校照片維持 `100vw - 48px` 寬、24px 圓角及左右相鄰照片，畫面改為 3:2，沿用各校裁切定位、線稿選校、四秒輪播與播放控制。
- 760px 以下拍立得由內容容器 92% 改為 84%，保留奇偶左右對齊、膠帶、角度、背景影片及翻面。正背面共用同一 Grid 格，依兩面內容共同撐高，字型與字級沿用；WebGL 繼續讀取實際 DOM 尺寸。
- 已定案的手機影片＋米白文字區、自然捲動，以及介紹區雙照片重疊維持原設計。桌機五校線稿沿用同日定案的 160px 版本。
- 本機 fixture 與 Chromium 驗證記錄、前後畫面在 `output/playwright/mobile-composition-20260922/`；修改前快照 `versions/before-mobile-composition-20260922-173616/`。已部署至正式 Nuxt，deployment `81f2ce4e-0722-4fbe-bc5d-125196351034`，release `050b9d12…`；固定快照／正式站七尺寸、觸控、翻面、五校圖片載入與文字放大證據在 `output/railway-mobile-composition-20260922-175717/`。Safari／iOS 實機尚未驗證，凍結 vanilla 原型不回寫。

## 官網後台第五輪 UX 定案（2026-09-24）

- 總覽的四格留給參觀案件：新需求待聯絡、待園方確認、今日參觀、到期待追蹤。有期限的事（待確認占位）要寫剩多久，不只給數字；草稿與通知失敗放在待辦清單。
- 「目前沒有待處理事項」只能在新需求、待確認、到期追蹤、草稿、通知失敗、未開放預約都為 0 時出現。
- 總覽連到案件列表一律帶最早送出在前（`order=oldest`）；列表的排序要能從網址帶入。
- 側欄只有「參觀案件」掛數字（新需求＋待園方確認），用暖黃實心膠囊；其他項目不加數字，避免側欄變成通知中心。數字與總覽同源，不另開計數 API。
- 有期限的案件在明細裡要寫「剩多久」與逾期後果，不能只給時間戳。

## 官網後台第四輪 UX 定案（2026-09-23）

- 總覽任何數字都要有同定義的入口：「到期待追蹤」連到 `/visit-requests?due=1`，後端 `follow_up_due` 與 dashboard 的查詢條件相同。日後新增指標比照，不能只給數字。
- 案件詳情是「處理一筆就接下一筆」的工作台：保留「下一筆待處理」、確認後預填聯絡紀錄；破壞性的「取消預約」放面板底部、有分隔線，不與主動作相鄰。
- 發布確認框列差異而不是時間戳；成功 toast 一定附「查看官網」。頁首「查看官網」開的是已發布版本，title 要說明草稿不會顯示。
- 文案：對話框否定鈕統一「先不要」（不可逆動作的確認鈕寫動作本身）；「圖片說明」不叫「替代文字」；不出現 eyebrow、樣板、版本號等工程語；標籤不能帶過期狀態（如「尚未開放」）。
- 後台操作色維持 09-22 裁定的青藍；審查指出它與官網綠金脫鉤，列為已知取捨，未翻案。
## 正式照片與手機構圖同步基準（2026-09-22）

- 拍立得不顯示照片補充字；保留替代文字與時間戳。`print-figure` 應與正方形 `print-photo` 同高，不得重新加入占位的 figcaption，否則 WebGL 依 figure 量測時會改變裁切。
- 手機拍立得維持容器 84% 寬、左右錯位及正背面共同撐高；消息與近期活動採原生橫向捲動，消息保留大圖與下方標題。
- 五校桌機選校列上限 1040px、線稿 160px；手機分校照片維持 3:2。圖片延後載入及既有 WebGL 效能設計保留。
- 上述為已部署快照 `a2a32307…` 的既有定案。同步 main 時需連同五個產品差異一起帶入，避免完整 CI 部署回退手動部署內容。

## 效能修復與動效實作規則（2026-09-22，Lighthouse 手機／SEO 修復）

視覺以正式原始碼為準，以下是為了效能改掉「怎麼做」而不改「長什麼樣」時定下的規則：

- **首屏 hero 底線**：不再用 `stroke-dashoffset` 沿路徑描畫（非合成屬性、每幀重繪 SVG），改成 `.hero-underline` 遮罩框向右滑＋內層 SVG 反向滑的雙層 transform 擦出；節奏、延遲、曲線與原本相同，減少動態直接顯示完成狀態。
- **簾幕幾何在 hydration 前就要預留**（`web/app/assets/css/performance.css`）：`useCurtain` 量出 `--belief-own`／`--day-own` 之前，track 用 `padding-bottom` 補捲動距離、下一段用負邊距先疊到同一位置（含 seam=2 與 760px 以下的變體），量完只是換成 sticky，不再位移。改簾幕的 `[data-motion="on"]` 規則時，performance.css 的 `:not([data-motion])` 版本要同步。
- **捲動驅動動畫只掛在真正會動的元素上**：五校→消息換頁與頁尾進場原本對整段 wrapper 的 inherited 自訂屬性（`--news-paper-progress`、`--home-footer-progress`）做 view-timeline 動畫，每幀整棵子樹 style recalc；原生路徑改成直接對 underlay／sheet／`::before`／`::after` 做 transform、opacity、border-radius、box-shadow 動畫（wrapper 只留 `timeline-scope`），變數只保留給 JS 備援（`useNewsTransition`／`useHomeFooterFade` 的 fallback）。首屏 `home-reveal-curtain`（clip-path）與 `home-reveal-copy`（color）維持原樣：改成 transform 會壓扁內容、改雙層交叉淡化中段色會與設計不同。
- **拍立得 WebGL（保留，不回 CSS 3D）**：觸控裝置貼圖 DPR 上限 1.5（2026-09-23 起改為全裝置最高 3×，見「日常照片畫質」）、shadow map 512、顯影改六格預繪交叉淡化（不逐幀 `ctx.filter`）、建場景分三段讓出主執行緒並用 `compileAsync`、WebGL 探測整頁一次、觸控裝置同時最多保留 2 張（`paper-budget.ts`，離視窗最遠的卸回 CSS 版、捲回再掛）；桌機維持「接近視窗立即掛載」時序；共用 renderer 持有一個暖身場景讓 shader program 不被釋放。
- **分校卡片**：只有當前與左右鄰卡綁 `src`／`srcset`（`loading=lazy` 擋不住橫向排在視窗外但在預載邊距內的卡），沒綁的卡保留 width／height 佔位；水滴進場與輪播時鐘不受影響。
- **字型與資源載入**：LINE Seed Bold 只有 `font-subsets.css` 這一份 `@font-face`（`scripts/subset-critical-fonts.py` 切成 critical／remaining 兩段 `unicode-range`，檔名帶內容雜湊），`styles.css` 不得再宣告整包 `lineseed-bd`——兩份都留時瀏覽器會同時下載 154 KB 整包與 150 KB remaining（2026-09-22 線上實測，首屏字型 171 → 343 KB、FCP 1.6 → 3.3 s）。critical 字集從實際 SSR 首屏文字推導（`scripts/first-screen-chars.cjs` 在 320–1440 六種視窗量首頁、五個分校頁、visit 的首屏 LINE Seed Bold 用字，2026-09-22 為 53 字／13.8 KB），只 preload critical；remaining 交給 CSS 自己發現。critical 的 `unicode-range` 必須含 `U+20`：CSS 行框高度取家族裡涵蓋空白字元的第一個 face 的度量，兩段都不含 U+20 時行框會改用 PingFang（1.40em vs LINE Seed 1.61em），所有標題往上跑 2–4px（截圖比對抓到）。h1 的 EB 與兩個品牌字子集照舊 preload；品牌字 `@font-face` 併進 `styles.css`，不再載 `brand-fonts.css`；`nitro.compressPublicAssets` 預產 br／gz、SSR HTML 由 `server/plugins/compress-html.ts` 壓 brotli。
- **`features.inlineStyles` 維持預設 true**：曾改 false 去掉 scoped 樣式 inline 又 link 的重複，線上 simulate 反而變差（阻塞渲染的 CSS 從 2 支變 4 支，Lighthouse 估多等 1.5 s）。字型修好後同機 A/B（各 3 次 median）分數打平（simulate 82 vs 83、LCP 都 4.05 s；devtools LCP 2.02 vs 2.27 s），inline 只多 28 KB（brotli）HTML、少兩個阻塞往返；線上是高延遲手機網路、往返比位元組貴，且線上早上 89–90 分的版本就是預設值，因此保留 true。錯誤頁（error-404／500）的 chunk 與 CSS 以 `build:manifest` hook 關掉 prefetch，不再跟著首頁載入。
- **分享圖**：`og:image` 用 `web/public/assets/og/<name>.jpg`（1200×630，`scripts/optimize-site-images.py` 的 `OG_IMAGES`），不用 WebP；首頁 JSON-LD 列五校 `Preschool`（地址、電話取自發布內容）。
- 已知但未改：hero h1 實際字重是 700（`h1,h2,h3{font-weight:700}` 蓋過 `.studio-hero h1{font-weight:800}`），與 2026-09-15「ExtraBold 800」不符且 EB 子集因此沒被用到；正式原始碼的分校區塊樣式（校名 38px、膠囊 tabs）與線上 build（Noto Serif 校名、底線式 tabs）不同，未在本輪對齊，待設計側裁定。

## 官網後台參考 ivy-frontend（2026-09-22 定案）

依使用者指定的參考專案及「幫我開始做」，官網 `admin/` 採園務後台的深藍灰導覽、青藍操作色與淺灰工作區。此裁定取代下方後台「淡綠中性色與深綠操作」規則；公開官網的品牌與設計維持各自定案。

- 參考 `ivy-frontend/src/assets/design-tokens.css`：側欄 `#1e2a3a`、選取底 `#314257`、選取字 `#7dd3fc`；accent `#0284c7`，文字與實心按鈕用深一階 `#0277b5`、hover `#0369a1`。以等值 OKLCH token 存放；綠色仍代表成功／已上線，暖黃仍代表草稿或待注意。
- 桌機側欄 260px、頁首基準 64px，沿用兩層分組收合、搜尋與開合記憶。手機使用原有 900px 抽屜斷點、44px 控制項及關閉後焦點返回。深色樣式只在側欄內覆寫 Element Plus 控制項，內容表單與 teleport 對話框保持淺色。
- 保留系統字、14px 基本文字及 8／12px 圓角；總覽摘要集中為一個白底面板，手機為兩欄。表格、表單、日期連結、素材選取、登入頁使用同套 token；不引入額外字型或 UI 套件。
- 導覽權限、五校範圍、CMS 發布與預約語意不變，不引入園務系統的 tenant／API／資料模型。此次只改樣式；未增加桌機圖示軌道收合功能。
- 修改前快照：`versions/before-admin-reference-20260922-161148/`。本機實際 Vue 介面搭配合成 API 驗證，截圖及腳本在 `output/playwright/admin-reference-20260922/`；Safari／iOS 實機未驗證。

## Nuxt 載入效能第一批（2026-09-22）

- 巡覽照片接近視窗才載入；縮圖依顯示尺寸使用 responsive 候選，放大與展開檢視使用原圖。保留 CMS 媒體 UUID 路徑、既有照片與熱點，不改成不同裁切。
- 首頁理念與日常簾幕在 hydration 前預留與動效相同的軌道與重疊距離；沒有 JS 或減少動態時保留靜態閱讀。日常封面跟隨閱讀位置載入，自動播放偏好與封面載入分開。
- 字型外觀與字重不變。Nuxt 的 LINE Seed 700 以互斥 unicode-range 切分首屏／其餘字形，只預載首屏包；字形聯集和字寬與原始子集一致。此規則取代下方歷史紀錄的「全站預載兩個完整字型」；凍結原型不回寫。（2026-09-22 稍後：首屏包改由實際 SSR 首屏文字推導為 53 字／13.8 KB、critical 的 unicode-range 補 U+20、`styles.css` 整包宣告移除，見上方「效能修復與動效實作規則」的字型一節。）
- 量測以本機 production build、fixture 與固定限速條件為實驗室證據；不等同正式站或真實使用者的 Core Web Vitals。影片 Range 與進一步壓縮列入第二批。

## 常春藤的一天移除照片補充字（2026-09-22 定案）

依使用者更正，六張拍立得照片左上角不顯示補充字及底標。DOM 與 WebGL 貼圖同步移除圖說繪製和專用樣式，先前的圖說改寫亦撤除；此裁定取代較早角落圖說字級及標籤規則。照片、時間戳、下方標題、背面故事與翻面操作保留，區塊來源說明沿用。CMS 原始資料及凍結 vanilla 原型不更動。

## 頁尾採用 A「深森林綠」（2026-09-22 定案；2026-09-24 已由上方 R「燕麥＋深綠底列」取代）

使用者確認 `design/footer-colour-directions-20260922/` 的 A，Nuxt 共用 `SiteFooter.vue` 採深森林綠底 `#24483F`、米白導覽文字 `#F5F2E7`、品牌字 `#FFF9E9`、暖金英文與「我們的大家庭」`#E3C77B`；標語與底列 `#C2D0C5`、分隔線 `#526D61`。

- 色票以元件內 scoped CSS tokens 定義，不改全域 `--paper`／品牌色，適用首頁、分校及預約頁的共用頁尾。保留原文案、字型、尺寸、留白、導覽與五校連結，以及底列空值時隱藏的行為。
- 首頁消息區延續原有漸退至暖白的動畫，再接深綠頁尾；此節取代較早「消息結尾與頁尾同為暖白」的配色要求。消息內容與動畫進度不變，不把消息字改成白字。
- 鍵盤焦點採暖金，滑鼠停留加底線；強制色彩模式使用系統色。凍結原型不回寫，三案比較頁保留為提案紀錄。
- 修改前快照：`versions/before-footer-colour-a-20260922-144730/`。

### 頁尾畢業版校徽（2026-09-23 加入，2026-09-24 使用者要求撤下）

- 2026-09-24：使用者要求拿掉頁尾校徽，頁尾品牌區回到只有中英文品牌名稱，不要再自行加回。1000px 以下品牌區跨整列、760px 以下導覽跨整列的排版保留；英文品牌名的 `lang="en"` 保留。
- 素材 `web/public/assets/ivy-graduation-crest.png` 保留在 repo（`design/footer-crest-journey-20260923/` 待拍板時可能還會用到），目前沒有頁面引用。
- 原始定案（已撤下）：畢業版雙童校徽放在頁尾中英文品牌名稱左側，桌機 72px、手機 60px。頁首校徽與凍結原型不受影響。

## 字體審查 B 批：明體統一、標點、字級尺度（2026-09-23 定案）

使用者看過 `design/typography-b-20260923/` 對照頁後決定四項都做，細項照對照頁的建議。只改 `web/`，凍結原型不回寫。

- **明體統一**：`Ivy Campus Serif`（自託管 Noto Serif TC 500）改為全站宣告，`--font-serif` 供首頁分校資訊、分校頁校名、預約頁大標共用。預約頁大標另切 9 字子集，不再落到 Windows 的新細明體。
- **分校頁校名**：由 LINE Seed 800 改為明體 500，尺寸與首頁分校資訊共用 `--fs-campus-name`（沿用 09-22 定案：桌機 50–66px 流體、1100px 以下 54px、700px 以下 50px），字距 .065em、行高 1.25。分校頁校名因此不再用到 LINE Seed ExtraBold（字檔仍供開場倒數使用）。
- **標點**：h1–h3 用 `text-spacing-trim:trim-start`，行首開括號收半格；Chrome 預設已收相鄰標點（」，）。**「，」「。」不收**：LINE Seed TW 原始字型的 halt 只含括號，逗號句號置中佔滿一格，維持台灣全形排法。WebGL 拍立得標題照字型 halt 數值（「 左移 .32em、字寬收 .5em）在 canvas 同步處理。
- **斷行**：分校頁 `.section-title` 與消息卡標題一樣 `keep-all`＋`balance`，只在標點後換行；太長時 `overflow-wrap:anywhere` 兜底。拍立得背面的 canvas 換行補上禁則（標點不放行首、開括號不留行尾），以前會出現「。」「？」單獨一行。
- **字級尺度**：固定字級只用 `typography.css` 的 12 階 token：`--fs-xs` 12、`sm` 14、`md` 16、`lg` 18、`xl` 20、`2xl` 24、`3xl` 28、`4xl` 32、`5xl` 40、`6xl` 48、`7xl` 60、`8xl` 64。新寫的 CSS 不要再寫 px／rem 字面值。
  - 例外保留原值：頁首／導覽／選單／膠囊／品牌字（園方 09-16、09-17 規格）、活動日期 36px、校名 `--fs-campus-name`、`clamp()`／vw 流體字級、11px 以下的英文裝飾字、開場動畫。
  - 收斂造成的變化大多是 ±1px（13→14、15→16、17→18、23→24、38→40）；明顯變動是頁尾校名與拍立得時間戳 22→24、活動與消息區標題 1100px 以下 30→32、理念大標 1100px 以下 42→40（09-22 原定 42）。
  - 表單「必填／選填」、預約頁地區與地址、窄螢幕五校頁籤等 10–11px 中文一併提高到 12px。

## 首頁字體角色分工（2026-09-22 定案）

使用者確認 `design/typography-roles-20260922/` 的分工提案，整合到正式 Nuxt 首頁及消息／活動清單、詳情視窗。此節取代這些區域原先共用 LINE Seed 標題的規則。

- 首頁主標維持 LINE Seed 700（桌機最大60px、390px手機34px）；品牌中英文字標、分校明體500與孩子的一天標題維持既有設定。
- 理念大標仍為 LINE Seed 700，桌機 `clamp(2.25rem,3.35vw,3.25rem)`、1100px以下42px、760px以下32px，行高1.5。正文系統400，桌機18px／手機16px、行高1.95、行長32em；內容與手機展開功能沿用。
- 消息區標題採系統黑體600，桌機32px／手機28px；文章標題20px／18px。活動名稱與清單、詳情標題同步使用資訊字體；詳情標題28px，正文16px。
- 英文小標及日期沿用 Source Sans 3 400，活動日期36px；日期欄隨 rem 伸展，避免文字放大後重疊。中文分類小標維持系統字。metadata13px、手機消息入口14px／500。
- 首屏 eyebrow／操作按鈕改為系統字500；主標字形、字級與版面保留。樣式集中 `web/app/assets/css/typography.css`，載入在既有樣式之後，不改全域 `--font-head`，不新增字型資產。
- 未整合「首頁與分校內頁銜接」mock-up。本次只採用字體方向，凍結原型仍維持原版本。
- **2026-09-23 補**：消息卡標題 `text-wrap:balance`＋`word-break:keep-all`（`overflow-wrap:anywhere` 兜底），只在標點後換行、不留兩字孤行；不要改回 `pretty`，因為它不會把兩個中文字當孤字處理。英文小字一律走 `--font-en`（＝`--font-date`，Source Sans 3），不要再寫沒定義的字型變數，因為 `var()` 失效時不會報錯，只會退回父層字型。


## 五校校園圖採用質感修復版（2026-09-22 定案）

使用者確認 `design/campus-photo-enhancement-20260922/` 五校修復版後要求替換，Nuxt 校區資料的 `image` 改用 `<原素材名>-enhanced-v1`，同步套用首頁分校輪播、分校內頁封面及分校分享圖。本節取代較早「不生成或更換素材」的限定；版面、線稿、輪播與裁切定位沿用原設定。

- 義華實際尺寸 1546×1017，其餘四校約 1737×906；母檔 WebP 沿用確認版，不再次生成或插值放大。由既有腳本產生 480／800／1200px 響應式衍生圖，檔名包含內容雜湊。
- 修復版帶有 AI 重建細節，不宣稱是原始高解析實拍。分校內頁圖說由「官方校園照片」改為「校園圖像」。
- 舊圖片保留供新聞與既有內容使用；校園探索的獨立場景圖片及定位點不改。PNG 母檔、原圖比較、完整提示詞與來源記錄保留於上述 design 目錄；凍結的 vanilla 原型不回寫。


## 手機 Hero 加高影片（2026-09-22）

依使用者追加「讓影片佔更多版面」，Nuxt 1000px 以下的影片高度由 `clamp(280px,43svh,420px)` 提高為 `clamp(320px,60svh,620px)`，沿用穩定閱讀視窗 `--motion-vh`，避免手機網址列伸縮時跳動。影片下方留白調為 24px，文字區下方留白為 36px；米白底、文案、無框漢堡與播放控制沿用。

影片加高後手機 Hero 採自然捲動，不在讀到文字與按鈕前啟動揭幕淡出；首次繪製也不預留手機 sticky 全屏空間。桌機 1001px 以上維持原有影片高度與揭幕轉場。

## 手機 Hero 米白文字區與無框選單（2026-09-22）

依使用者要求移除手機 Hero 下方深綠面板。Nuxt `web/` 在既有 1000px 以下分段版面中保留上方校園影片，下方採 `--paper` 米白底、`--text` 深色文字，移除白字用陰影；既有主標、黃底線、兩個入口與影片控制維持原位。原生及 fallback 揭幕動畫均維持深色字，按鈕框與鍵盤焦點採 `--green`。

首頁上方漢堡選單移除外框與內距，兩條 22×2px 線水平／垂直置中；保留 44×44px 點擊範圍、展開叉號與鍵盤焦點。此節更新下方明亮版紀錄中的手機深綠底設定；桌機影片版面及凍結原型維持既有設計。

## 頁尾移除開發輔助文字（2026-09-22 定案）

Nuxt 共用頁尾不再顯示「標題字型 LINE Seed TW」及「機構原官網」連結；主頁尾導覽與五校入口保留。版權與頁尾備註皆空時，省略整個底列及其分隔線、留白；字型素材與授權文件保留。

## 分校標題 B 與放大明體校名（2026-09-22 定案）

使用者選定 `design/campus-heading-20260922/` 的 B，追加「校名前不要數字、分校資訊換字體、校名放大並換字體、重排下方資訊」。已套用 Nuxt `CampusBoard.vue`；本節取代下方圓角輪播規格中的標題／校名字型及標題橫向分散排列。

- 中文「分校資訊」與英文 Campuses 同列置中，下方五校選單也置中；保留 B 的上下細線與當前校區底線，不加入編號。標題採石墨藍灰 `#3D5057`，英文暖灰褐 `#786C5C`。
- 中文標題和大校名使用自託管 Noto Serif TC 500（思源宋體）；英文副標 Georgia italic，聯絡文字與五校選單維持易讀無襯線字。13 字子集逐字驗證涵蓋分校資訊與五校名，附 OFL、來源記錄；不覆寫既有 LINE Seed 或品牌字型。
- 大校名桌機 50–66px、平板 54px、手機 50px。資訊帶加入上緣細線，桌機校名／聯絡／預約三欄垂直置中，聯絡欄左側分隔線建立界線；平板預約移至聯絡欄下方；手機依校名、聯絡資訊、滿寬預約按鈕排列，英文留在大校名下方。
- 保留照片、校區／預約路由、社群來源、四秒輪播與水滴控制器；缺 LINE 繼續顯示「待園方提供」。凍結原型不回寫。
- 修改前快照 `versions/before-campus-heading-b-20260922-114141/`。Chrome 320／390／768／1024／1440／1920px × 五校、置中／觸控尺寸／字型載入／鍵盤與路由，共 32 筆驗證通過，另 10 項桌機／手機自動播放、暫停、離屏及減少動態檢查通過，無 runtime error；Nuxt typecheck 與原型語法／重打包通過。截圖及紀錄 `output/playwright/campus-heading-b/`；Safari／iOS 實機尚未驗證。

## 官網分頁圖示採用既有校徽（2026-09-22 定案）

- favicon 使用頁首相同的彩色校徽：原始 `logo.png`、`viewBox="30 26 124 132"` 與 `logo-colour-cutout` 透明處理，等比例置於正方形圖示；保留皇冠、人物、枝葉與 IVY KIDS 緞帶。
- 提供透明 16／32／48px ICO、48px PNG 與白底 180px Apple touch icon。小圖示不含右側中英文字標，不採用尚未定案的 30 週年提案。

## 最新消息到頁尾 A「霧藍漸退」（2026-09-22 定案）

使用者選定 `design/footer-transition-20260922/` 的 A，整合至 Nuxt 首頁。保留既有消息紙頁進場，頁尾進入畫面後，消息背景逐漸變成與頁尾完全相同的 `--paper` 暖白；紙頁陰影同步歸零，避免殘留灰色交界。

- 進度以頁尾從畫面底部進入到完整可見為 0–1；覆色透明度為 `clamp(0, progress × 1.65, 1)`，與選定預覽一致。頁尾高於視窗時，以一個視窗的進入距離計算；反向捲動會還原。
- 消息內容、活動卡顏色、頁尾文字與連結維持原來排列，不新增釘選、位移、淡字或捲動留白。暖白覆色只在背景層、`pointer-events:none`，不影響消息與頁尾操作。
- 首頁 `index.vue` 提供獨立 footer view timeline；`HomeNewsTransition.vue` 同時接收原有進場及新頁尾進度，兩者不共用同一個動畫變數。原生支援需同時通過 timeline、entry range、timeline scope；其餘由 `useHomeFooterFade` 的 passive scroll／rAF 備援。
- 和既有首頁共享 `motionViewport`，手機僅改變可視高度時不重建閱讀進度；離開首頁會移除 listener、observer 與待執行 frame。減少動態直接顯示暖白，強制色彩不加裝飾覆色。
- 只套首頁，不更動分校／預約頁的共用 SiteFooter。探索頁保留作方向紀錄，不把比較工具帶進官網。
- 修改前快照：`versions/before-footer-fade-a-20260922-095616/`；整合驗證：`output/playwright/footer-fade-a/`。
- Node 22 typecheck、六項閱讀高度單元測試通過；Chrome 六尺寸 320–1920px 的正反向捲動、原生／備援、手機觸控／高度變動、頁面往返與減少動態／強制色彩共 48 筆驗證通過。原有紙頁轉場另 47 筆回歸檢查通過，零 runtime error；Safari／Firefox／iOS 實機尚未驗證。

## 分校資訊圓角影像輪播（2026-09-22 定案）

使用者確認 `design/campus-rounded-20260922/` 後整合至 Nuxt `CampusBoard.vue`。本節取代下方較早的左右分景、宋體校名與六秒輪播規格。

- 照片成為區塊主體：中央大幅、左右露出相鄰校園；桌機寬 80vw／上限 1200px，1600px 以上為 78vw／上限 1440px。桌機圓角 28px、手機 24px；700px 以下左右留 24px，照片近正方形。保留各校 `panoramaPos` 與響應式圖片，不生成或更換素材。
- 標題與五校選單在上方，照片下方依序為控制列、校名／地址電話／預約入口。移除圖說、張數、獨立「認識○○校」按鈕與前後圓鈕；照片和校名仍可進入校區內頁。LINE／Facebook 使用網站既有品牌圖示，社群文字不加外連箭頭；缺少 LINE 繼續顯示「待園方提供」。
- 中文採 PingFang TC／Microsoft JhengHei，標題和校名 500；英文與電話沿用 Source Sans 3 400。區塊專用暖瓷白底、墨綠字與香檳金 CTA，不覆寫其他區域品牌 token。
- 四秒自動輪播、可暫停；校名、圓點、鍵盤左右／Home／End、照片左右滑動都能切換。依 2026-09-22 最新裁定，照片進入畫面後自動開始，滑鼠停在照片、校名或控制器上都持續播放，不再以滑鼠停留暫停。鍵盤聚焦、照片離屏或分頁隱藏時仍暫停；減少動態預設停止，可手動開始。零校不渲染、一校不顯示控制列；CMS 校區變更時保留仍存在的已選校區。
- 進度膠囊與播放鍵使用原生 `position:sticky; bottom:max(12px,env(safe-area-inset-bottom))`，限定在照片與控制列共同的 `.gallery-media` 中。首次進場前保持隱藏；已顯示後回捲時可靠畫面下緣，回到自然位置後隨照片離開，不覆蓋聯絡資訊。保留 `#campuses` 與 `.campus-panorama`，接續既有「孩子的一天」捲動接縫。
- 控制列進場依使用者指定的 [Apple iPhone 官網](https://www.apple.com/iphone/) 水滴動態：小水滴上浮、壓扁回彈，伸展成膠囊並分離出播放鍵，圓點／圖示稍後出現；總長 1.2 秒，邊框在形狀分開後顯現。依 2026-09-22 最新截圖裁定，首次滑到照片下方控制列的自然位置、該區塊至少 80% 可見時才播放；以不隨 sticky 移動的定位元素觀察，避免照片才露出就提前觸發。回捲不重播，不改既有四秒輪播計時。裝飾底板變形不改排版，初始位移按當下實際尺寸量測；縮放／旋轉或鍵盤聚焦中止進場並直接顯示完整控制器，減少動態／高對比直接顯示。SSR 預設為可見靜態控制列，保留原有按鈕語意與點擊範圍。
- 首頁「預約參觀○○校」入口常駐，導向 `/visit/:key` 並預選該校；實際預約頁繼續讀取 CMS 預約模式。本輪只實作入口顯示，不更改正式預約開放狀態。
- Chrome 六尺寸 × 五校 30 個版面、鍵盤／觸控／四秒播放、桌機與手機停靠及消息紙頁遮蔽通過；typecheck 與輪播／預約動作 15 項測試通過，Safari／iOS 實機未驗證。整合證據：`output/playwright/campus-rounded-site/`；修改前快照 `versions/before-campus-rounded-integration-20260922-092539/`。凍結的根目錄原型不回寫。

## 分校資訊版面與閱讀優化（2026-09-21 晚間）

本輪延續無線稿 B 與膠囊進度，更新下方「膠囊進度與版面細修」的排版：

- 桌機左資訊 40%，上限 640px，其餘留給照片；901–1100px 左資訊 48%。校名 48–72px，地址 16px、電話 24px，社群 14px；小字使用較深的 `--campus-muted`，對米白底為 4.84:1。區塊標題中文在前、英文在後。
- 水平校名維持至少 48px 高，已選取以淡綠底、深綠字與 2px 底線標示。Grid 欄數依實際校區數量，不保留未發布校區的空欄。
- 舞台採 `copy / photos / controls` Grid areas；控制列在正常排版中，有獨立細分隔線，取代絕對定位與左欄固定底部 padding。預約訊息、文字換行或開放預約按鈕增加高度時，控制列自然往下。
- 900px 以下照片置上；761–900px 資訊為雙欄，左側校名與分校入口、右側聯絡方式，控制列橫排；760px 以下資訊單欄、五校仍一列。手機地址與社群 14px、說明至少 12px；英文副標改用 Grid，不用絕對定位。圖片 `sizes` 與 900px 斷點同步。
- 保留六秒自動循環、暫停續播、滑鼠閱讀、鍵盤、離屏與減少動態規則；手動暫停時明示「已暫停，可點選校名切換」，不被 hover 狀態覆蓋。播放鍵有 title 與控制區關聯。
- 圖說使用局部深綠底膠囊，移除照片底部整片漸層；沿用真實照片與既有 `panoramaPos`，不更換或生成素材。2048／768／390px × 五校的文字背景像素抽樣最低 7.89:1。
- 本輪僅改 `web/app/components/CampusBoard.vue` 與文件；凍結原型重打包後無差異。45 個版面狀態、觸控、鍵盤、輪播與開放預約的本機驗證在 `output/playwright/campus-refine/`。

## 五校底板卡 e3（2026-09-18 定案，取代「五校分區輪播」與「分校資訊 B 全幅橫景」的舞台）
- 六輪比稿（`design/campus-scroll-directions/`：滿版捲動 a／b／c → A 線稿放法 a1／a2／a3 → a1 版面 a1／a1b／a1c → d 底板卡 → d1／d2／d3 比例 → e1／e2／e3 卡片對背景）後，使用者選 **e3 浮卡跨色帶**，已併進站內，`?campus=` 分流與比稿用程式碼移除；改前快照 `versions/before-campus-board-e3-20260918-231737/`。
- **外框不變**：米白底、置中「CAMPUSES／分校資訊」、右上五校線稿浮水印（跟校區切換）、「← 五校軌道 →」（`.campus-picker`／`.campus-track`／`.campus-seg`）。
- **舞台 `.board-stage`**：`min(1320px,100%)` 置中、上圓角 24px；照片 2.4:1（`--panorama-pos` 逐校）自帶 24px 圓角；區塊背景改成上米白、下 300px 墨綠的 `linear-gradient`（`.campuses.campus-board{--band-top}`），照片卡騎在色帶上緣，底板 `.board-plate` 落在色帶裡不另畫框。
- **底板一列**（`grid: auto 1fr auto auto`）：校名（宋體 1.5–1.875rem）＋「高雄 · 地區」（金）｜所在地、參觀專線（金色圖示）｜LINE／Facebook 單色細圈徽章（32px、hover 轉金、待補帳號虛線圈、第二行小字隱藏，`campusLinks()` 產出不變）｜暖黃「預約參觀Ｘ校」＋「在 Google 地圖開啟 ↗」文字連結。**不放地圖、不留「認識Ｘ校」**（校區頁入口改由頁首選單、頁尾與校名軌道承擔）。
- ≤1100px 底板改兩欄（資訊｜按鈕，社群橫排）；≤760px 照片 4:3、上一／下一校浮在照片右上、底板單欄、色帶 420px。
- 行為沿用 `setupCampusShowcase()`：tablist 鍵盤、首尾循環、200ms 淡出淡入、6 秒自動循環與暫停規則、`#campus-live` 播報；`apply()` 改用 `boardInfo()`／`boardActions()`。示意地圖 `CAMPUS_MAP`／`campusMap()` 與舊舞台樣式（`.campus-stage-details/-info grid/-name/-contact/-side/.campus-map-stage`）已刪除；各校內頁仍用 Google 嵌入。
- 驗證：`output/playwright/campus-board-e3-{desktop,tablet,phone}[-last].png`；五校循環、分校頁、回首頁重建、無 console 錯誤、無水平溢出。

## 離開首屏的浮動膠囊（2026-09-18 已上站）

**2026-09-22 Nuxt 內距更新**：依使用者截圖，`web/` 的 `--pill-pad` 由 4px 改為 8px，四邊增加留白；控制項尺寸不變，含框高度桌機 58px、900px 以下 62px。此裁定取代下述舊版 4px 內距；根目錄凍結原型保留。

**2026-09-21 Nuxt 定案更新**：依使用者截圖指示，`web/` 首頁展開選單也採用與膠囊一致的深綠毛玻璃（`--pill-bg`：墨綠 74% 不透明度、16px blur、1.3 倍飽和度），保留米白主字、mint 次字、亮色細框與現有排版。以 `@supports` 啟用，不支援濾鏡或 `prefers-reduced-transparency:reduce` 時使用 `--pill-solid`。桌機維持 420px，窄螢幕以 `max-width:calc(100% - 32px)` 保留左右各 16px，避免裁切電話。此裁定取代下述 2026-09-18「選單卡不透明」的舊設計；已凍結的根目錄原型不回寫。

首頁捲過首屏（hero）後，整條頁首讓位，改由置頂靠右的墨綠毛玻璃膠囊接手：白色圓徽章的校徽、「選單 Menu」、暖黃「預約參觀」（2026-09-18 起校名與英文字不進膠囊，只留校徽；校名保留在 `.pill-brand` 的 `aria-label`）。導覽四個項目收進膠囊的選單卡。提案與三個方向的比較留在 `design/header-collapse-directions/`（A 同組收合／B 浮動膠囊／C 極簡列＋全幕選單），園方選 B。

- **只作用在首頁**（`body:has(.studio-hero)`）。分校頁與預約頁的頁首、手機展開列都沒有改，因為那裡的頁首本來就是米白實底、不是壓在照片上的透明列。
- **觸發點：往下捲的第一步（2026-09-18 改）**。`scrollY > 40`（`COMPACT_AT`）就切換成 `data-state="compact"`，捲回最上面還原成完整頁首。原本要等整段首屏揭幕結束（約一個視窗高度）才收，園方要求一動就收。`is-scrolled` 用同一個門檻，避免先變米白頁首再收成膠囊的兩段跳動。
- **首屏頂部遮罩跟著收**：`.studio-hero::before` 第一層是為白色導覽字設計的 190px 暗帶；收合後導覽字已經不在照片上，用 `background-size:100% 0` 把它收掉（可過渡，0.35 秒）。不收的話深綠膠囊壓在暗帶上對背景只有 1.08 的對比，等於看不見邊界；收掉後桌機 1.50–2.78、手機 2.08–4.49。膠囊在收合狀態另外把外框提到白 28%、陰影加重，只改 `--pill-border`／`--pill-shadow` 變數，不直接寫 `box-shadow`，否則會蓋掉左右分離版「膠囊本身不投影、由子元素投影」的設定。
- **毛玻璃（2026-09-18 晚定案）**：膠囊底色 `rgb(var(--ink) / .74)`＋`backdrop-filter:blur(16px) saturate(1.3)`，邊框白 28%；74% 是壓在首頁最淺的米白上仍讓米白字 6.5、`--mint` 英文 4.7 的下限（58% 只有 3.9／2.8，英文不過 AA）。選單卡**不透明**，走 `--pill-solid`（`--green`），因為壓在照片上半透明的導覽字不好讀。園方在 explore.html 六方向裡選了毛玻璃。
- **為什麼是深綠**：首頁捲過首屏後的五段背景都是淺色（淡綠關於、米白孩子的一天、米黃分校與消息、米黃頁尾），米白／白／淡綠膠囊與它們的邊界對比只有 1.0–1.4，浮起來全靠陰影；深綠 `--green` 對每一段都有 7.1–9.6。膠囊上米白字 9.87、`--mint` 英文 7.13、黃底深綠字 9.66。四種色調與三檔尺寸的實地比較在 `design/header-collapse-directions/b-tuning.html`。
- **尺寸（2026-09-18 晚縮一檔，園方覺得原本太大蓋到內容）**：桌機膠囊高 50（含框，內距 4＋控制項 40）、頂距 12、圓角 999、靠右、寬度隨內容（1440px 實測 259，縮前 299×62）；徽章 36／校徽 28；選單字 13px／Menu 10px（曾短暫收掉只留兩條線，同晚拿回；膠囊實測 259×50）、預約鈕 14px 內距 16；校名不顯示（原本校名 Noto Sans TC 600 16px、英文 Source Sans 3 11px，樣式仍留在 `.pill-name` 規則備用）；選單鈕與預約鈕 40 高。900px 以下控制項 44（膠囊 58，手機未跟著縮）、左右各留 16、選單鈕只留兩條線。
- **選單卡（2026-09-18 晚改）**：至少 420 寬、右緣對齊膠囊右緣、與膠囊下緣留 8px、四角圓 24、有邊框，同色深綠（膠囊縮到 220 後不能再同寬，否則導覽字擠成兩行；膠囊打開時維持全圓角，不再改成平底）；四個導覽項目兩欄、五校膠囊一列、義華校參觀專線一行。位置由 `app.js` 依膠囊量測，手機還在首屏時改貼頁首容器（`.from-bar`，圓角 24）。
- **更不擋內容的探索（2026-09-18 晚，待拍板）**：`design/header-collapse-directions/explore.html` 比六個方向（現況／`?autohide=1`／`?pill=edge` 貼視窗右緣／毛玻璃（已套預設，參數移除）／`?pill=bare` 三顆散開／`?pill=dock` 右下停靠），截圖在 `explore/`。我建議 autohide、園方選毛玻璃（選單卡改實色補可讀性）；bare 失去整體感、dock 撞到孩子的一天的「暫停背景」鈕。
- **選單就是手機的導覽**：首頁在 900px 以下，頁首的漢堡與膠囊的漢堡開的是同一張卡，不再用 `.navigation.open` 展開列；兩顆漢堡打開時都變成 ✕。其他頁面仍用展開列。
- **無障礙**：兩顆漢堡共用 `aria-expanded`／`aria-controls="menu-panel"`；打開時焦點進第一個連結，Esc、點外面、點連結、回到首屏都會關閉並把焦點還給按鈕。隱藏的那一組（首屏的膠囊、收合後的頁首）用 `visibility:hidden`，不會被 Tab 到。收合後 `.header` 設 `pointer-events:none`，只有膠囊與選單卡接收點擊，看不見的頁首不會擋住內容。
- **選單鈕的特異性陷阱**：站內既有 `body:has(.studio-hero) .menu-toggle span{height:2px;width:22px}`（特異性 0,2,2）會把膠囊裡的 `.menu-lines`、`.menu-word` 一起壓成 22×2 的白條；膠囊內任何 `span` 的規則都要明確寫出 `width`／`height`／`background`，光靠加前綴不夠。
- **位置：靠右（2026-09-18 定案）**。膠囊右緣貼內容欄右緣，和原本頁首的預約鈕同一條線；`--pill-gutter` 跟著 `.container` 走（1101px 以上 `max(48px,(100%-1280)/2)`、1100px 以下 32px，斷點取自 studio.css 第 16 行而非 styles.css 的 1150）。選單卡與膠囊同寬、右緣對齊。900px 以下不受影響，仍是左右各留 16 的滿版膠囊。
  先前的置中版與其他擺法留作比較，用 `html[data-pill]` 切換（`index.html?pill=center|left|split|split-mini#/home`），比較頁 `design/header-collapse-directions/placement.html`。
- **向下捲收起、向上捲出現**：預覽參數 `index.html?autohide=1#/home`，mock-up 與對照動畫在 `design/header-collapse-directions/autohide.html`。只在收合狀態作用，方向門檻 6px，一次跳超過 240px（錨點跳轉、按 End）只更新基準不改狀態；選單開著、焦點在膠囊裡、接近頁首、捲到底、減少動態時一律維持顯示。**尚未套成預設**，站上仍是一直顯示。
- **驗證（2026-09-18）**：桌機 1440／手機 390 的首屏→收合→開選單→Esc→回首屏全數正確；1920 到 320 共 12 種視口無水平溢出、膠囊完整在畫面內、觸控目標 ≥44px；減少動態下膠囊與選單卡直接出現；分校頁與預約頁沒有膠囊、手機仍是原本的展開列；`preview.html` 單檔同步。Safari 與 Firefox 尚未實機。

## 頁首雙語導覽字排（2026-09-17 定案）
- 園方確認採用此版，套用於首頁、分校頁與預約頁共用頁首，並同步單檔預覽。
- 依園方要求，四個導覽項目與預約鈕保留中文在上、英文在下。桌機中文 15px、英文 Source Sans 3 400／12px，行間距 2px；四個導覽項目最小寬度 88px，對齊兩行基線。窄桌面與手機另調整字級及間距。
- 選取、hover 與鍵盤焦點的底線放在整組文字下方，不穿過中英文間距。英文維持繼承字色，移除文字陰影；透明首頁與捲動後淺色頁首沿用既有配色。
- 手機選單維持上下雙語、靠左對齊；英文 12px，預約鈕英文 11px。英文子集擴充至 ASCII 並隨 `preview.html` 內嵌。此節取代早期「導覽列不放英文副標」規則。
- **30 週年版待拍板（2026-09-18）**：1997 創校、2027 滿 30 年。校徽是 PNG，記號只能掛在字標旁：a 校徽等高的印章／b 英文行下第三行「30 週年 ── 1997–2027」／c 中文校名右上角的上標小籤，用 `html[data-anni]` 切換（`index.html?anni=a|b|c#/home`），比較頁與取捨在 `design/anniversary-directions/`。金色只用 `--gold`，首屏影片上鏤空、淺底實心；膠囊與手機三版共用校徽右下角的金色「30」小點。
- **預約鈕：金色滿高色塊（2026-09-18 定案，已上站，全站共用）**。原本那顆和四個導覽項共用第 21 行的雙行排版、只多一個框，會被讀成第五個導覽項，圖示還用了語意相反的打勾。現在是暖黃實心、與頁首同高、**貼齊視窗右緣**、左下一道 44px 的弧把視線收回內容；圖示換成 `#i-calendar-check`（膠囊那顆同步換）。捲動後的膠囊預約鈕本來就是暖黃，整條動線只剩一種長相。首頁壓在影片上、分校頁與預約頁壓在米白頁首上，兩邊同一顆。
  - **貼邊靠的是 `.header-top` 改滿版**：901px 以上把它從置中容器換成 `width:100%` ＋ 左邊補回 gutter（1101px 以上 `max(48px,(100%-1280)/2)`、901–1100 明寫 32px）。不能寫成 `.header-book` 的負邊距——自訂屬性裡的百分比是在**用它的元素**上解析的，對到的是 `.header-actions`（約 200px），`max()` 會一路退回 48px。滿版規則要包在 `@media(min-width:901px)`，別在 `max-width:900` 裡還原容器寬度，那會蓋掉 760／380 各自的 gutter。代價是頁首那條底線跟著拉成滿版。
  - **900px 以下退回金色膠囊**：手機右邊還有漢堡，色塊貼不到邊，`height:auto`＋`border-radius:10px`，`.header-actions` 收回 `align-self:center`。
  - **人在預約頁時**這顆指向原地，視覺照舊但會帶 `aria-current="page"`（`render()` 裡跟著 href 一起設）。是否要在該頁把它降級成低調樣式，尚未決定。
  - 兩輪比稿（a–e 選色與形制、d1–d12 形狀）的截圖與取捨留在 `design/book-cta-directions/`，`?book=` 參數與調校頁已隨定案移除。另注意 `studio.css` 那組「捲動後預約鈕變色」在首頁看不到——`is-scrolled` 與 `data-state="compact"` 同時觸發、頁首同時收起，那組只對分校頁與預約頁有意義。

## 品牌區版面與筆電捲動動畫修正（2026-09-17 上午回饋）
- 品牌區改為上方橫列標籤、下方左文右圖。照片獨立成欄，不再塞在右側主欄的子欄內；容器最大 1400px，文字／照片約 1.05:1，900px 以下改為直向閱讀。標題採 52–80px，正文自然換行；縮小並淡化「用心／陪伴」背景字，統一上下分隔線與留白。
- 桌面 100% 縮放沒有動畫的原因是舊首屏最小高度 740px，使 720／730px 視窗不通過內容高度檢查；另有 JS 與 CSS 的 680px 高度停用門檻。改為視窗高度的首屏，短桌面收緊上下間距，移除固定高度停用門檻；只有內容實際放不下、減少動態或 quiet 模式才改為一般捲動。
- 1000px 以下保留短捲動距離與文字上移；依新標籤列重新調整品牌區上方留白，避免轉場中主標太早被裁掉。此節取代下方早期的三欄比例與 680px 停用規則。
- 驗證紀錄與截圖：`output/playwright/home-reveal-refine/`。修改前備份：`/private/tmp/ivy-reveal-layout-before-20260917/`。
- Chromium 驗證：1512×720、1440×680、1280×650、1280×600 都由舊版停用恢復為捲動揭幕；2048／1440／1280／1000／768／390／320px 版面無水平溢出。原生／JS 備援、反向還原、離開首屏暫停影片、跨頁返回、減少動態與 720×450 內容放不下時的自然排列通過。Safari／Firefox 尚未實機驗證。

## 首頁向上揭幕與淡綠品牌區（2026-09-17 定案）
- 使用者確認 `design/scroll-reveal-mockup/` 後套用主站。現有校園影片、首屏文案、導覽、孩子的一天、五校與消息功能沿用；「關於常春藤」改為淡綠大字理念、低對比裝飾字及孩子照片。
- `home()` 將首屏與品牌區包進 `.home-reveal`；首屏釘選在 `.home-reveal-track` 內。影片淡入米白、標語縮淡，再由底部往上裁切露出品牌區。桌面捲動距離為畫面高度的 72%，1000px 以下縮為 38%；文字分別縮到 90%／96%。手機與平板文字在淡出時同步上移畫面高度的 20%，讓轉場中仍能看見淡字，初始首屏位置維持原樣。
- 優先使用具支援偵測的 CSS view timeline，明確設 `view-timeline-inset:0`，不讓導覽的 scroll-padding 改變動畫起點。不支援時以 passive scroll + requestAnimationFrame 更新裁切、透明度及縮放，沒有增加動畫套件。
- 影片、文字與按鈕仍使用原始 DOM；完全被遮住的首屏與淡出的操作區會設 inert。影片離開畫面暫停，返回時恢復原本的播放意圖，手動暫停不會被覆蓋。切換路由時清除動畫 frame、ResizeObserver、scroll／resize／媒體偏好 listener；影片來源依原有流程移除。
- 減少動態、視窗高度 680px 以下、首屏內容比視窗高，或 `?hero=quiet` 時採正常文件排列。減少動態的即時變更會停止影片。既有 about／life／campuses／latest-news 錨點與焦點定位保留。
- Chromium 驗證：1440×900、1280×800、1024×768、1000×800、768×1024、390×844、375×812、320×740、720×450，無水平溢出；原生與 JavaScript 備援的揭幕／反向還原、減少動態、影片手動暫停、分校／預約跨頁返回、四個首頁錨點通過，未出現 JavaScript 錯誤。Safari／Firefox 尚未實機驗證。
- `node --check app.js`、`git diff --check` 通過；重新打包並直接以 file URL 開啟 `preview.html`，桌面／手機轉場正常，照片與影片為 data URL，未請求額外本機 assets。畫面紀錄在 `output/playwright/home-reveal/`。
- 本節取代先前首頁品牌區的白底等寬雙欄配置；mock-up 仍保留作設計參考。

## 分校資訊 B「全幅橫景」（2026-09-16 定案）
- 使用者從 A–G 提案選定 B，視覺參考 `design/campus-layout-mockups-20260916/b-panorama.png`。首頁改為置中標題與底線式五校選單、全寬校舍照片、下方「校名／聯絡方式／預約與地圖」三欄；保留前後切校、地圖選校、鍵盤與 6 秒自動輪播。
- 五校校舍線稿移到標題右上並同步切換；移除原右側獨立樹木與圍牆裝飾。內頁聯絡區保留各校線稿，亦移除共用花園背景。
- 桌面容器最大 1560px，照片比例採 2.6:1，比生成提案略高以容納現有照片的屋頂與立面；761–1200px 資訊重排為兩列，760px 以下直向排列、照片採 3:2。義華橫幅定位 `center 12%`，沿用使用者指定照片；其原圖僅 590×388，寬螢幕放大清晰度受原素材限制。
- 標題及校名採本機宋體系字型 `Songti TC / Noto Serif TC / PMingLiU / serif`，以區塊專用 token 呈現 B 的字形；正文與頁首維持既有字型。分頁採文字與底線、照片圓角收至 6px，資訊欄以 1px 細線分隔。
- 行動按鈕有獨立更新節點，與照片、資訊、線稿同步替換；地圖保留原 SVG 與真實聯絡資料。小地圖調整 viewBox 顯示五校；1200px 以下將規劃路線放在地圖下方，避免覆蓋校名。
- 驗證與截圖在 `output/playwright/campus-panorama-b-*`。修改前備份：`versions/before-campus-panorama-b-20260916-231334/`。本節取代下方舊有左右雙欄與左下線稿配置。
- 完成驗證：五校 × 2048／1440／1024／768／390／320px 均無水平溢出，照片、線稿、校名與預約連結一致；地圖五個圖釘、鍵盤、箭頭循環、手機預約帶入校區、快速切換、自動輪播、五校內頁與強制色彩模式通過。`preview.html` 的五校照片與線稿均為內嵌資料，沒有額外本機 assets 請求或 JavaScript 錯誤；語法與 diff 空白檢查通過。

## 五校專屬線稿套用（2026-09-16 晚間更新）
- 首頁分校資訊依選取校區顯示義華、明華、崇德、國際、仁武各自的建築線稿；分頁、左右箭頭、地圖選校、鍵盤與自動輪播共用同一更新流程，照片與線稿先預載後同步替換。連續選校及離開頁面時沿用切換序號，避免舊載入結果覆蓋新選擇。
- 各校內頁「交通與聯絡」同步套用該校線稿。國際校使用園方提供的既有線稿截圖，其他四校使用本次製作的獨立素材，來源與生成提示詞保留在 `design/campus-line-art/`。
- 網站素材為 `assets/campus-line-art-{yihua,minghua,chongde,international,renwu}.webp`。右下樹木與圍牆沿用 `assets/campus-line-art.webp`，遮罩隱藏其中原本固定的國際校建築。
- 延續 C 版米白底、左下校舍與右下樹木；760px 以下線稿縮到區塊底部留白，聯絡資訊有同底色保護。線稿為裝飾圖片，以 `aria-hidden`、空 `alt` 與 `pointer-events:none` 排除朗讀及操作；強制色彩模式隱藏裝飾。
- 本節取代下方 C 版定案紀錄中「固定國際校線稿」的素材配置。`preview.html` 以同一圖片查找函式內嵌五校素材。
- 驗證：首頁五校於 2048／1440／768／390／320px 均正確載入，無水平溢出；五校聯絡區於 1440／390px 正確。鍵盤、箭頭、地圖點選及強制色彩模式通過。檢查與截圖在 `output/playwright/campus-art-switch-*`；修改前備份在 `versions/before-campus-art-switch-20260916-220650/`。
- 一般動態模式的快速連續切換、自動輪播及切換途中離開頁面通過；單檔預覽的首頁與五校聯絡區皆使用正確 data URL，沒有本機 assets 請求或 JavaScript 錯誤。結果保留於 `output/playwright/campus-art-switch-preview-results.json`。

## 義華校封面照片（2026-09-16 晚間更新）
- 依園方提供的校舍照片，首頁分校資訊與義華校內頁封面改用 `assets/yihua-exterior.webp`（590×388，原圖無損轉存）。首頁裁切定位 `85% center`，內頁橫幅定位 `85% 8%`，保留右側城堡與塔頂。
- 原 `assets/campus.webp` 仍用於孩子的一天及含既有定位點的校園探索。新素材已同步內嵌到 `preview.html`；原始上傳檔保留在 `versions/before-yihua-photo-20260916-215035/user-provided-yihua.png`。

## 分校資訊背景 C（2026-09-16 定案）
- 使用者從三款 mock-up 選定 C「校舍輪廓線稿」；參考圖 `design/campus-background-mockups/c-campus-line-art.png`。
- `.campuses` 改為低彩度米白 `--campus-surface:oklch(97.9% .011 94)`；左下為國際校建築線稿、右下為樹木與圍牆，作為品牌裝飾，不作為其他校區的實景資料。
- 背景素材 `assets/campus-line-art.webp`（1938×812，63,932 bytes）由內建 image_gen 依確認的 mock-up 製作；原圖與提示詞留在 `design/campus-background-mockups/`。CSS 偽元素承載、`pointer-events:none`，不加入朗讀或 Tab 順序；強制色彩模式隱藏線稿。
- 背景貼齊區塊下緣；聯絡欄使用同底色與柔和邊緣，讓裝飾不穿過文字。760px 以下線稿縮到區塊底部，底部留白 128px；沿用現有五校版型、照片和互動。
- 打包器支援 CSS 中的本機圖片 URL 內嵌，`preview.html` 同步更新。
- 驗證：2048／1440／1280／1024／768／390／375／320px 無水平溢出、聯絡欄在視窗內；背景皆已載入且不攔截點擊。鍵盤切到仁武校後預約正確進入 `#/visit/renwu`；單檔背景為 data URL，無額外 assets 請求、無載入失敗或 JavaScript 錯誤。
- 截圖與檢查程式：`output/playwright/campus-line-art-*`。本次修改前備份：`versions/before-campus-line-art-20260916-175906/`。

## 頁首中英文品牌字排（2026-09-16，依園方規格）
- 上排「常春藤教育機構」：Noto Sans TC 600，桌面 30px（1.875rem），行高 1.2。
- 下排「Ivy Educational Institution」：Source Sans 3 400，桌面 18px，行高 1.25。依後續「英文緊湊、仍與中文對齊」回饋，字距由 .08em 收至 .02em，並放大字級，減少單字間的空隙；維持與上排等寬、左右對齊。
- 桌面徽章寬 56px，圖文間距 14px；依「中英上下靠近」回饋移除原本 4px 的額外排距，以既有行高保留文字之間的留白。
- 依園方提供的品牌圖取樣，中文使用深綠 `#075B30`、英文使用深藍 `#29266D`（先將截圖的 Display 色彩描述檔轉為 sRGB，再取文字內部像素中位數）。CSS 以等效 OKLCH 專用品牌 token 表示。首頁未捲動、文字疊在影片上時兩行都使用白色；捲動後的淺色頁首與內頁保留綠／藍字色。品牌文字停用陰影。
- 1100px 以下中文為 24px；760／380／360px 以下分別為 18／16／15px，英文維持中文字級的 60%。徽章與圖文間距一併縮小，保留 44px 回首頁點按範圍。
- 指定字型以品牌用字子集內嵌在 `assets/fonts/brand-fonts.css`；執行 `python3 package_preview.py` 同步產生此檔與 `preview.html`。本節取代下方歷史紀錄的頁首圖片字標與字型設定。

## 釘選導覽列淺色版（2026-09-16）
- 首頁往下捲動後，頁首使用實色暖白底、深綠文字，下緣採兩層柔和陰影（近處 2px／5px、遠處 8px／20px）增加層次。依 2026-09-16 園方回饋移除半透明與 backdrop-filter，避免 Logo 原圖白底和背景產生矩形色差。
- 目前導覽項目使用繼承字色與 2px 底線，修正原本深綠底上的綠色文字不易辨識；透明首屏也保有清楚的白色選取狀態。
- 釘選狀態的 Logo 顯示原圖黑色字標與彩色校徽，停用白字濾鏡與疊圖，以 multiply 融合原圖白底；頁首建立 isolation，讓 Logo 只與實色頁首混色。首屏透明導覽沿用既有呈現。
- 預約參觀在釘選狀態改為實心深綠鈕，保留勾勾、10px 圓角與既有縮小尺寸；手機選單及鍵盤焦點也改用深綠。
- 頁首高度與導覽位置保持穩定，桌機 92px、手機 78–80px。

## 首屏明亮版（2026-09-16，依確認的 mock-up 實作）
- 提案來源：`design/hero-bright-mockup/hero-bright-desktop.png`。正式頁面沿用原始校園影片、poster、Logo 和桌面 60px 主標，沒有把生成圖替換成背景素材。
- 桌面遮罩只留兩層：上方 190px 的導覽漸層、左側文字漸層。左側遮罩在畫面寬度 62% 處完全透明，移除原底部暗帶與右下角徑向暗層；底部校區文字列一併收起，校區資訊仍可由上方「五所校園」進入。
- 主標維持「在常春藤，每一天都有新發現。」；黄色手繪線只畫在「新發現」三字下，句號留在外側且不單獨換行。1001–1279px 的主標隨視窗縮小至 44–60px，使文字留在左側遮罩內。
- 副文採確認的兩句：「一起讀故事、做作品，也到戶外探索。／在老師的陪伴下，動手試試，說出自己的想法。」桌面 18px、400 字重、最大 28em，兩句各自成行；375／390px 仍為兩行，320px 自然排成三行，不裁字。
- 1000px 以下影像置上、深綠文字區置下，照片高度 `clamp(280px,43svh,420px)`；影像只保留導覽漸層。小尺寸主標採 30–34px，320px 頁首預約鈕維持一行及 44px 高度。
- 播放／暫停改為 44px 圓形圖示鈕，只在按鈕內加底色；保留動態的螢幕閱讀器名稱、鍵盤焦點、手動播放、減少動態與影片失敗備援。
- 驗證尺寸：1440、1280、1024、1000、768、390、375、320px，均無水平溢出；1440×900、1280×800 首屏與播放鈕收在一屏。720×450 的 200% 縮放等效 CSS 視窗也可正常重排。
- 對比抽樣：1440、1280、1024、390px 各取 12 個等距影片時間點，隱藏文字與陰影、保留實際 CSS 遮罩和按鈕背景，計算每段 DOM 文字範圍內最亮背景像素。跨尺寸最低值：大字主標 3.30:1、副文 5.56:1、導覽 5.32:1、播放圖示 4.67:1，通過本次抽樣門檻；不是所有影片逐幀或全站 WCAG 認證。標準：[W3C Contrast Minimum](https://www.w3.org/WAI/WCAG21/Understanding/contrast-minimum)。
- 1440px 截圖中 x=894–1440、y=190–900 區域，開啟／移除遮罩後逐像素一致，確認右側與下緣沒有整片暗層。
- 單檔 `preview.html` 已重新打包並驗證，CSS、字型、照片、影片皆內嵌，沒有請求本機 assets；播放／暫停、Space 鍵、手機選單、Escape、孩子的一天定位、影片載入失敗和無文字預覽正常，JavaScript runtime error 為 0。
- 證據：`output/playwright/hero-bright-audit.json`、`hero-bright-final-check.json`、`hero-bright-desktop.png`、`hero-bright-mobile.png`。備份位於 `versions/before-hero-bright-20260916/`。

## 首屏品牌敘述（2026-09-16 更新）
- 依園方提供的 UC San Diego ArtsConnect 文案語氣，首頁改以「在常春藤，每一天都有新發現。」開場，接續遊戲、閱讀、藝術創作與自然探索的中文敘述。
- 內文移除手動斷行，以 `max-width:32em` 限制行長並使用 `text-wrap:pretty`，不支援的瀏覽器採自然換行。沿用既有標題字型、手繪底線與「看看孩子的一天」入口。
- 首頁瀏覽器標題與單檔 `preview.html` 同步更新。
- 依園方要求移除首屏「常春藤 · 校園生活影片／影像」文字與對應的狀態更新，保留播放／暫停控制並在手機靠右排列。
- 頁首「預約參觀」文字前加入既有 Phosphor `check` 勾勾圖示，圖示以 `aria-hidden` 排除於朗讀名稱之外。
- 頁首預約鈕縮小外框留白：桌面 padding 改為 `9px 24px`，最小高度 `44px`；手機沿用既有留白與至少 `44px` 點按高度。

使用者確認方向：大器框架＋溫暖品牌細節。沿用原型的雙層頁首、滿版主視覺、內容分欄與校區內頁；米白、深綠主色，暖黃重點。品牌 logo 使用原始圖檔，完整等比例。中文採系統 PingFang TC / Microsoft JhengHei，清楚、穩定的中文字形，減少巨大英文標題。
首頁依序：影片封面、品牌介紹、五校總覽、教育理念、校園活動、參觀入口。影片素材尚未提供，使用義華校照片作封面，保留可配置的 video source。
高擬真互動 prototype：首頁＋五校介紹頁＋選校後填表流程，無真實資料提交。
Image gate: skipped. 現有 prototype 框架、兩個官網參考與色彩方向均經使用者確認，並非開放式構圖探索；直接依已確認的版面製作。


## 第二道區塊簾幕：關於常春藤 → 孩子的一天（2026-09-18）
- 使用者指定「切換區塊的滾動動畫」要和首屏一樣：上面那一段停住不動，由下往上被擦掉，露出下一段。首屏 → 關於已經是這樣（`.home-reveal`），這次在關於 → 孩子的一天之間加第二道（`.belief-reveal`）。
- 與首屏的差別：首屏本來就只有一個畫面高，可以整段 sticky；關於常春藤比視窗高（1440×1000 約 1228px），所以改成**把底部釘在視窗底部**（`top:min(0, 視窗高 − 區塊高)`，由 `setupBeliefCurtain()` 量測後寫進 `--belief-stick`），前面的內容照常捲動，只有最後一個畫面被擦掉。
- 軌道 `.belief-reveal-track` 高度＝關於區塊自身高度＋72svh 的擦拭距離；擦拭用 `clip-path:inset(0 0 進度×視窗高 0)`，孩子的一天以 `margin-top:-100svh` 墊在下面，擦完時它的頂端正好對齊視窗頂端（五種寬度實測誤差 0–1px）。
- 擦完後關於區塊設 `inert`，鍵盤不會 Tab 進看不見的內容。偏好減少動態時整段回到 `still`：不黏、不擦、照原本上下排列。
- 導覽錨點：`.day-experience` 加 `scroll-margin-top:-112px`（與關於區塊同樣做法），否則 `#/home/life` 會落在擦拭未完成的位置，畫面上方殘留一條淡綠。
- 曾誤把「關於常春藤」的大字做成孩子的一天那種淡出（2026-09-18 稍早），使用者指正後已全部還原，浮水印回到固定 .36／手機 .37、進場空間回到原值。

## 孩子的一天：拍立得顯影（2026-09-18 定案，取代下一節）

### Nuxt 版改用 WebGL 紙張翻面（比稿 R，2026-09-21 併進 `web/`）

- 使用者裁定把第八輪 R「紙張翻面」（three.js）用在 Nuxt 官網；vanilla 原型仍是零依賴的 CSS 3D 版，不回寫。
- 實作在 `web/app/utils/paperPrints.ts`，由 `DayMomentCard.vue` 在卡片接近視窗（rootMargin 60%）時動態載入 three（獨立 chunk，gzip 約 180 KB，不進首屏）。**整頁只開一個 WebGL context**：共用 renderer 畫完再 `drawImage` 到每張卡自己的 2D `canvas.paper-view`，不採 R 的每卡一個 context。
- 貼圖的字型、顏色、字級與版位一律從 DOM 的 computed style 與 `getBoundingClientRect()` 取，不寫死 hex；DOM 正反面 `opacity:0` 但保留版面、鍵盤、`aria-expanded`／`inert`，整卡翻面感應區與紙膠帶（`z-index` 8／6）浮在畫布（4）之上。
- 保留的手感（翻面參數已由下方 2026-09-23 一節取代）：翻面 1.1 秒緩動、中段依 x² 彎曲並抬升 34px、環境光＋暖色平行光投影到後方地板、游標點光源與 ±0.28／±0.2 rad 微傾、顯影 3.2 秒在貼圖裡逐幀重畫、停留段落的時間戳亮一階。
- 退場規則：偏好減少動態、無 WebGL、three 載入失敗、圖片載入失敗都回傳 null，卡片維持 CSS 3D 版；`webgl-ready` class 走 Vue `:class`（手動 `classList` 會被 patch 蓋掉，已踩過）。卡片寬度改變時整組重建。
- 已知取捨：貼圖文字換行用 `measureText` 逐字折行，與 DOM 換行可能差一字；文字不可選取；headless 驗證走 SwiftShader，實機 GPU、Safari／Firefox 未驗證。
- 經 `design/day-timeline-directions/` B–R 十七版比稿後，使用者選定「顯影拍立得＋固定影片背景＋大字淡出」（該目錄 Q 版），本節為搬進主站的版本。原分頁圖文版見下一節，已整段被取代；備份 `versions/before-day-polaroid-20260918-160145/`。
- **版面**：區塊以 CSS grid 疊三層。固定影片背景（`assets/day-film.mp4`，800px 以下換 `day-film-mobile.mp4`）`position:sticky` 佔滿視窗；上層是內容。大字「常春藤的一天」同樣 sticky，相片以 `-28svh` 負邊距從它上面滑過。
- **大字淡出**：參考 Wellington College 首頁。`setupDayExperience()` 依相片區頂端與視窗的距離，把整個標題區的不透明度從 1 淡到 0.16（相片距離視窗 62% 處開始、25% 處淡完），之後標題留在相片後面當底紋。
- **相片**：方形拍立得比例，相紙顏色隨時刻（`--print-tint`：早安與回家暖黃、探索與午後薄荷、用餐蜜桃、午休奶油），上方一條同色系紙膠帶。進入視窗才開始顯影：照片從乳白低對比微模糊用 3 秒轉為清晰，橘色時間戳延遲 1.8 秒最後浮現，`.is-active`（讀者停留的那一段）再亮一階。
- **翻面**：正面是一句手寫標題，背面是橫線紙，寫故事與家長可以問的問題與說明。2026-09-21 使用者要求移除 C「角落貼籤」按鈕，Nuxt 版改為右下相紙折角＋無底框的「點照片，看看背面」提示；翻後改為「再點一下，回到照片」，手機常駐。**同日晚間第二輪（`design/flip-cue-directions-20260921/`，使用者選 3）**：紙色漸層折角在相紙上看不見，改成「折角做真」——相紙右下角真的切掉（CSS 版 `clip-path`、WebGL 版貼圖 `destination-out`＋`alphaTest`），掀起的那片是背面橫線紙、翻到背面換成正面相紙色，尺寸由 `--ear` 統一（靜止 44px）；顯影完成後折角自己掀一次 30→52→44（JS 單一時鐘同時餵 DOM 與畫布），CSS 版桌機 hover 掀到 54、WebGL 版不做 hover 放大。提示字提高到 13px／500、深綠、虛線底，圖示改為正面左右翻（`arrows-left-right`）、背面 U 型回頭（`arrow-u-up-left`），手機不再縮字。**首張偷看**：使用者裁定推翻本節原本的「不自動翻面」——只有第一張、掀角結束 0.3 秒後向左微翻 12° 再回正（1 秒），每次工作階段一次（`sessionStorage` `ivy-day-peek`）、已翻開不做、減少動態不做；其餘五張仍不自動翻。相紙陰影從 `.print-face` 搬到會跟著翻的 `.print::after`（clip-path 會把自己的 box-shadow 裁掉）。以透明原生 button 覆蓋整卡提供單一點擊／觸控／Enter／Space 入口，鍵盤 focus-visible 框住整卡，保留 `aria-expanded`／`aria-controls` 與隱藏面的 `inert`，不自動翻面。提示位在標題下方的獨立留白，不壓縮標題寬度。WebGL 與減少動態的 CSS 版共用提示；vanilla 原型維持凍結。
- **時刻**：08:00、09:10、11:40、12:30、14:20、16:30，取自官網 <https://www.ivykidschool.com/一日流程>；官網另有 08:30、15:30、16:00、16:10、18:00 等時段，本區塊只呈現有照片的六個。相片為義華校既有素材，與背景影片非同一天拍攝，區塊底部保留來源說明。
- **無障礙與節制**：偏好減少動態時不顯影、不傾斜、不自動播放背景，翻面直接切換；背景影片可手動播放／暫停，離開視窗或切換頁籤自動暫停，載入失敗則隱藏控制鈕只留 poster。離開首頁時 `disposeDay()` 卸掉捲動與 observer 並釋放影片。
- 驗證：1440／1024／768／390／320px 無橫向溢出、無 console 錯誤；六張相片顯影、鍵盤翻面與 `inert`、點相片翻面、減少動態、路由離開再回來、`#/home/life` 錨點、單檔 `preview.html`（11.9 MB，影片只內嵌一份行動版）皆通過。腳本 `output/playwright/day-site/check.cjs`。

### A「無文字淡折角」定案（2026-09-22，取代上一輪提示字；折角已於 09-23 晚由「A 角落捲起」取代）

使用者選定 `design/flip-without-copy-20260922/` A，並要求折角不明顯、翻面時自然且沒有元素停在原位。Nuxt 六張拍立得移除正反面的可見翻面文字；整張原生 button 與 accessible name、aria-expanded、aria-controls、inert 保留。折角常態 32px（比稿原為 76px），只露出淡橫線紙，陰影減弱，進場輕掀為 26→38→32；不再以 hover 放大折角。

折角屬於紙張本身：CSS 放在正反紙面內，WebGL 直接畫進相同貼圖，所以折角與紙張一起旋轉、彎曲，背面落在物理對應的左下角，不留固定在右下的 DOM 浮層。CSS 改為 1.1 秒無超出終點的緩動；點擊中止掀角與偷看，翻面途中暫停游標傾斜，WebGL 連點反向時依當前角度延續彎曲與抬升。先前的 mock-up A 同步修正折角層級與大小；B／C 留作比稿紀錄。

### 翻面手感改得更自然（2026-09-23，覆蓋上面兩節的翻面參數）

使用者要求翻面「更自然一點」。逐格錄影後找到的問題與改法（共用節奏在 `web/app/utils/printFlip.ts`，有單元測試）：

- **方向一致**：折角在右下、首張偷看也是右緣掀起，但舊版翻面是左緣掀起往右翻，方向相反。改為**永遠右緣掀起往左翻**，背面翻回正面也一樣（位置以半圈累加 0 → -1 → -2，停穩後收回 0／-1）；翻到一半再點則原路翻回。CSS 版由元件給 `--flip`（0、-180deg、-360deg…），取代 `.is-flipped{--flip:180deg}`。
- **節奏**：舊的 easeInOutCubic 點下去前 250ms 幾乎不動。改為 0.95 秒 `cubic-bezier(.28,.1,.38,1)`：點下即起步、約 1/3 時間立起、後段慢慢放平；CSS 與 WebGL 同一條（測試會比對 `styles.css`）。
- **紙的硬度**：舊版兩側對稱彎成 U 形、邊緣彎到約 76px，像軟布。改成單側懸臂：手捏的那一側平直，遠端依轉速落後（約 2.6Hz、略欠阻尼的彈簧），停下時輕輕回彈一次；抬升 36px 比轉角早到晚退，下緣（折角那側）先起來約 4.6°；抬得越高影子越淡越散。
- **紙膠帶跟著紙翻**：舊版膠帶是紙外的 DOM，翻面時停在原地浮空。現在 CSS 版把膠帶放進 `.print`（`translateZ(1px)`），WebGL 版照 `rotate`、`clip-path`、`--tape-a`／`--tape-b` 畫進貼圖（網格上緣多留一條透明帶）；翻到背面時被紙擋住，只露出超出上緣的那一截。
- **尺寸錯位（舊 bug）**：WebGL 量紙用 `getBoundingClientRect()`，吃進卡片 `rotate:-2.2deg` 的外框，紙比 DOM 大約 5%（460×580 畫成 482×597）並往右下偏，所以焦點框與點擊區對不上。改用 `offsetWidth/Height`，量測時也拿掉卡片、膠帶與標題的 rotate。
- **假摺痕（舊 bug）**：翻到一半時紙有半邊穿過 z=-26 的接影子地板，被地板蓋住的部分疊上自己的影子，看起來像一道摺痕。地板改排進不透明佇列最先畫、不寫也不比深度。
- 其他：懸停抬升原本是瞬間跳 10px，改為平滑；微傾與光點依實際經過時間平滑（高更新率螢幕不會變快）；鍵盤焦點框是平的矩形，翻面途中淡掉、停穩再出現；滑鼠／觸控點擊後立即放掉按鈕焦點（2026-09-23，否則之後按鍵捲頁會冒出綠框、空白鍵會再翻一次），只有鍵盤操作才看得到焦點框，**不要整個拿掉 outline**；CSS 3D 版翻面時整張微放大 3.5% 再放下。減少動態仍是直接切換。
- 驗證與前後逐格對照在 `output/playwright/flip-natural-20260923/`；改版前快照 `versions/before-flip-natural-20260923-082344/`。凍結的 vanilla 原型不回寫。

### B「捲動飄角」定案（2026-09-23，疊加在 A 淡折角上；同日晚由下一節「A 角落捲起」取代）

使用者要更不明顯或更有創意的翻面暗示，`design/flip-hint-subtle-20260923/` 三版（A 對光透字、B 捲動飄角、C 包邊貼紙）中選定 B。紙膠帶只黏上緣，所以捲動時下緣的折角被風掀起：

- 捲動速度（px/s，和上一筆各半平滑）→ 折角疊加 0～20px（32→52，約 2200 px/s 掀滿），整張順著方向微擺 ≤0.7°；停止捲動 60ms 後速度每秒剩 2%，彈簧略欠阻尼（折角 ζ≈0.47、擺動 ζ≈0.41），停下回彈一次、約 1 秒收回，回彈最低到 26px。常數與純函式在 `web/app/utils/earGust.ts`（`tests/ear-gust.spec.ts`）。
- **不自動播放、只回應使用者的捲動**；手機滑動一樣有效；減少動態不訂閱。沒有 hover 效果（09-22 已否決 hover 放大折角）。
- 六張感受到的速度相同，只算一組彈簧：一個 scroll 監聽＋一個 rAF 時鐘，畫面附近（上下 10%）的卡片才訂閱，離開即收回。
- 折角 = 基準（進場掀角 `runPeel()`／靜止）＋捲動疊加，由 `DayMomentCard.vue` 的 `renderEar()` 同時寫 DOM `--ear` 與 WebGL `paper.setEar()`，兩個來源不搶同一個值。
- 微擺寫在 `li.day-print` 的 `--sway`，`.print-card` 的傾角改走 `--card-tilt`、`rotate:calc(var(--card-tilt) + var(--sway))`；`paperPrints.ts` 量紙時本來就把 `.print-card` 的 rotate 拿掉，擺動中接手 WebGL 也不會量歪。之後改卡片傾角請改 `--card-tilt`，不要直接寫 `rotate`。
- 效能：WebGL 每次折角變動會整張重傳正反面貼圖（與顯影期同級成本）。M2 GPU 實測兩張 WebGL 卡在畫面內、折角掀到 47.6px 時 p95 16.8ms，與沒有拍立得的區段相同；手機捲動中本來就是 CSS 版。SwiftShader 量不出效能，驗證腳本預設用 Metal。
- 驗證 `output/playwright/ear-gust-20260923/`（`check.cjs` 功能、`perf.cjs` 幀時間）；改版前快照 `versions/before-ear-gust-20260923-143901/`。A／C 未採用，留作比稿。凍結的 vanilla 原型不回寫。

### A「角落捲起」定案（2026-09-23 晚，取代上面的淡折角與捲動飄角）

使用者要翻面提示有「風吹的感覺」：先比 `design/flip-wind-20260923/`（第一版誤解成折角被風拍動，使用者更正為「把折角拿掉，改用風掀起來」），選 A 後再比依 A 做的翻面三版 `design/flip-corner-turn-20260923/`，最後裁定 **A 角落捲起＋點下去也從右下角先捲，翻面效果維持不變**（翻面三版 A1／A2／A3 皆未採用）。

- **沒有折角**：平常是完整平貼的相紙。`.print-ear`、`--ear`、貼圖挖角（`cutEar`／`drawEar`）與進場掀角 `runPeel()` 全部移除，勿再加回折角。
- **捲動起風**：捲動速度→風力（約 2100 px/s 風滿；起 70ms、收 600ms 另加每秒線性 0.1 切掉長尾），觀者看到的右下角沿 45° 折線被掀起，範圍 50→130px（以 460px 寬為準等比），強風時翻過 90° 露出背面橫線紙；落回牆面時反彈 25%；整張順風微擺 ≤0.4°。每張卡在共用的風上乘自己的陣風起伏與顫動（種子不同），六張不同步。只回應捲動、不自動播放；減少動態不訂閱。常數與純函式在 `web/app/utils/cornerWind.ts`（`tests/corner-wind.spec.ts`）。
- **彎曲剖面** θ(u)=hinge·(1−(1−u)³)+tip·u³：轉角集中在折線附近、外側近乎一整片，tip 為負＝末端逆風回彎。比稿時用過 φ·u^p（越到末端越彎），只有尖端翻得過 90°，從正面看像缺了一角、看不出背面。沿邊的波幅超過 0.2 會像布不像相紙（現用 0.12）。數學在 `web/app/utils/cornerCurl.ts`（`tests/corner-curl.spec.ts`）。
- **點擊翻面**：點下去約 0.1 秒內角先捲到 1.3 rad，翻到 0.45 收掉；翻面本身（0.95 秒、同一條 cubic-bezier、單側懸臂彎、抬升、方向）不變，捲曲只疊在起步那段。翻到一半再點是原路翻回、不再起手；翻面途中風的捲曲在前 30% 淡出。起手捲的是點下去那一刻朝向觀者的那一面。
- **進場輕掀**取代舊掀角：顯影完成後角落輕掀一次（0.55 rad、1.1 秒）；首張偷看維持，在輕掀後 0.3 秒（首張偷看 09-24 起由下一節 F 取代）。
- **WebGL 版**：網格 28×28→44×56；變形＝角落捲曲（在觀者看到的那一面座標裡算，翻面途中固定起點那一面）＋原本的懸臂彎，背面網格由正面結果逐列鏡射；紙已是平的就不重畫。光源、貼圖、翻面節奏都沒動（比稿時為了讀出彎曲把環境光降到 0.9，正式版沒有跟進，以免改到翻面的明暗）。
- **CSS 版**（手機捲動當下、無 WebGL）：沒有網格可彎，用兩片 3D 三角紙近似（靠折線那片轉 θ(0.25)、角尖那片再多轉到 θ(0.75)），邊長 20cqw（`.print-wrap` 設 `container-type:inline-size`）；只有起風／起手／輕掀時才加 `.is-cornering` 把兩面各自的右下角切開換上三角片，平常紙面完整、不多開圖層。受光依角度算（光從左上前方）；forced-colors 不切角。兩面紙色抽成 `--print-face-bg`／`--print-back-bg`。
- 效能（M2 Metal、兩張 WebGL 卡在畫面內）：幀時間 p50／p95 16.7／16.8ms，與沒有拍立得的區段相同；第一陣風起手有一格約 50ms（第一次變形），之後沒有。
- 驗證 `output/playwright/flip-wind-corner-20260923/`（`check.cjs` 桌機 WebGL／強制 CSS／手機／減少動態，`css-lead.cjs` CSS 起手真實時間取樣——CSS transition 不吃 Playwright 假時鐘，`perf.cjs` 幀時間）；改版前快照 `versions/before-flip-wind-corner-20260923-210501/`。凍結的 vanilla 原型不回寫。

### F「第一張翻開進場」定案（2026-09-24，取代首張偷看）

使用者附「05 午後玩耍」靜止截圖，問怎麼提示可以翻面、但要自然。診斷：現行三個暗示（捲動起風、顯影後輕掀、首張偷看）都發生在捲動中或剛進場，讀者停下來看的時候卡片完全靜止；輕掀只到 0.55 rad（約 31°）、偷看只轉 12°，都看不到背面。比稿 `design/flip-hint-natural-20260924/` 兩批六版，使用者選 **F**。

- **做法**：每次工作階段第一次來到這一區，第一張（01 早安入園）載入後在畫面外悄悄翻成背面朝上（`.is-snapping` 關掉 transition，不播翻面）；讀者看得夠多（可見高度達卡片與視窗較矮者的 60%）並停留 0.8 秒，它自己翻成照片（右下角先捲起、右緣掀起往左翻，與點擊共用 `turnPrint()`），照片接著顯影。翻開前不顯影、也不做進場輕掀；其餘五張照舊。純函式與常數在 `web/app/utils/printOpener.ts`（`tests/print-opener.spec.ts`）。
- **SSR 與無 JS 維持正面朝上**，hydration 後才翻成背面；**載入當下第一張已在畫面內**（錨點 `#day-hello`、回上一頁、捲動還原）就不做，免得正面一閃變背面。
- **每次工作階段一次**：沿用 `sessionStorage` 的 `ivy-day-peek`（舊偷看的 key）。自己翻開、讀者先點、減少動態下看過，都算示範過。
- **讀者先點**：示範算完成，照片翻過來開始顯影，計時器不會再翻一次。自動翻開不放焦點、不算讀者翻過。自己翻開途中（0.95 秒內）被點不算，讓它翻完，不照一般規則原路翻回背面（`tapDuringOpen`；部署前在正式 build 實測發現，讀者正想看照片時會被翻回去）。
- **減少動態**：不翻，停在背面等讀者點（等於比稿 D）；看過就記下，下次載入正面朝上。
- **首張偷看已移除**：`DayMomentCard.vue` 的 `runPeek`／`isPeeking`、`paperPrints.ts` 的 `peek()`／`PEEK_*`、`styles.css` 的 `.is-peeking` 與 `@keyframes card-peek` 全部刪除，勿再加回。
- 看過未選：A 讀到才掀（停留 1.2 秒，角落翻過約 160° 露出背面；只掀到 117° 時角落側對鏡頭看不到背面，要改用 (1−u)⁶ 的緊捲剖面）、B 區塊引言「每張相片翻過來，都寫著那一刻的故事，和參觀時可以問的事。」、C 正面抄一行家長提問、D 04 安靜片刻常駐背面朝上、E 每張背後黏一張稍大的橫線便條露出紙邊。
- 驗證 `output/playwright/flip-opener-20260924/`（`check.cjs`：桌機 WebGL 假時鐘逐格、強制 CSS、手機 390、減少動態、同工作階段重新整理、讀者先點、錨點直達）；改版前快照 `versions/before-flip-opener-20260924-204501/`。凍結的 vanilla 原型不回寫。

### 日常照片畫質（2026-09-23，僅 Nuxt）

- 五張影片截圖使用已確認原片的原生 1080×800，取樣點保持 35.9／37.9／16.9／30.3／19.4 秒；保留人物、鏡頭、上方裁切與原色。母圖 WebP quality 94，衍生圖 quality 92，不從舊縮圖放大。原生尺寸以版本化檔名直接複製、不重壓縮。
- 拍立得維持方形與 50% 40% 裁切位置；響應式選圖把橫向原圖被裁掉的兩側計入像素需求。紙面貼圖、WebGL renderer 與顯示 Canvas 同步使用最高 3× DPR，保留既有顯影、翻面與備援。
- 教室空間參考仍為既有 960×600，午休實拍待補的說明保留；不以重繪替代園所真實紀錄。
- 五張完整圖片總計約 453 KiB，沿用近視窗載圖／延後 WebGL 初始化；凍結原型不回寫。重製腳本：`scripts/restore-day-photos.py`。

## 孩子的一天：開放式圖文專題（2026-09-16，已由上一節取代）
- 依使用者提供的精簡方向改版，保留原有六個片刻、照片、主標、正文與家長問答；日常情境不代表已核定的五校共同作息，用餐及午休仍標示教室空間參考與照片待補。
- 六個圓形圖示、連線改為橫向文字分頁：16px，間距 28–32px，選取項目以深綠色與 2px 底線表示；沒有外框或膠囊底色。保留方向鍵、Home／End、單一 Tab 停駐點與 tabpanel 關聯。
- 區塊採接近白色的暖灰 `--day-surface`，大標 36px。桌面圖文欄寬為 3:2，間距 64px，1100px 以下為 48px；兩欄上緣對齊，右側內容從照片頂部以下 20px 開始自然排列。
- 照片固定 8:5，無疊字、遮罩、陰影或圓角，圖說位於下方。右欄收斂成 28px 主標、17px 正文、可展開問答；取消多層標籤、章節序號、已探索進度及上一段圖示鈕。
- 校園連結移入右欄，與正文左界對齊，並保留簡單的「下一段：片刻名稱 →」；最後一段可回到早安入園。常駐的 sr-only 區域只播報目前片刻與標題。
- 760px 以下改為單欄；文字分頁可橫向滑動、露出後續選項，切換後選取項目留在可見範圍。手機點下一段會回到新片刻影像；不新增進場或輪播效果。
- 圖片及標題起點在各片刻間保持穩定；問答展開時左側圖片不會拉長。樣式集中於 `styles.css`，移除 `studio.css` 的舊卡片覆寫。

## 可讀性與觸控底線（2026-09-10 審查後定案）
- 中文說明文字（圖說、頁尾、麵包屑、提示）最小 12px（.75rem）；只有裝飾性英文小標可低於此值。
- 所有可點元素高度至少 44px；行內文字連結用 `padding-block` 搭配負 `margin-block` 撐出觸控範圍，不改變版面。
- hero 只設 `min-height`、不設固定 `height`，文字放大到 200% 時不被裁切。
- 深色遮罩、淺色文字、錯誤色、時間軸顏色一律走 `--ink`、`--mint`、`--error`、`--trail` token，不再散落 rgba／oklch 字面值。

## 中文優先與箭頭規則（2026-09-10 定案）
- 段落小標（.eyebrow）一律中文短語，對應導覽或子導覽的名稱；英文只保留在品牌名（頁尾 IVY KINDERGARTEN、logo）。導覽列不再放英文副標。
- ↗ 只用於離開本站的連結（機構原官網、義華校活動頁、Google 地圖）。
- → 與 ← 只用於有順序的操作：預約表單的上一步／下一步、孩子的一天的前後片刻。其餘按鈕與文字連結不加箭頭。

## 圖示（2026-09-10 定案）
- 全站使用 Phosphor Regular 單一套圖示，經 Iconify 取得後以 SVG sprite 內嵌在 `index.html` 的 `<body>` 開頭；JS 以 `icon('name')` 輸出 `<use>` 引用，不引入任何 runtime。
- 圖示只出現在動作旁：電話、地圖、預約、表單步驟、片刻切換、照片探索工具列、外部連結。不做「標題上方一顆大圖示」的排版。
- 尺寸跟隨文字（1.1em），顏色跟隨 `currentColor`。「孩子的一天」於 2026-09-16 改用純文字分頁，移除時段圖示與面板序號；只在下一段操作保留箭頭。摺疊指示（FAQ 與片刻問答）維持字元。
- 新增圖示：到 Iconify 查 `ph:` 前綴的 Regular 版本，把 body 加進 sprite 的 `<symbol id="i-名稱">`。

## 標題字型（2026-09-10 園方選定方向 B）
- 標題（h1／h2／h3、頁尾品牌名、大字電話）改用 LINE Seed TW：一般標題 Bold 700，hero 標語 ExtraBold 800。內文、引言、表單、按鈕維持系統字（首屏 eyebrow／按鈕的例外見 2026-09-15 節）。
- 字型以子集 WOFF 自行託管在 `assets/fonts/`，頁尾註明「標題字型 LINE Seed TW」；`font-display:swap`，並在 `<head>` 預載兩個字型檔。
- 三個方向的比較畫布與產生器保留在 `design/font-directions/`。


## 創作工作室 demo（2026-09-13）
使用者確認以簡約、時尚、藝術與創新方向製作一版 demo。新增 `studio.css` 作為主題層，首頁在 `app.js` 的 `home()`。
- 單層頁首，桌面 92px；900px 以下收合導覽。
- 暖白底、深綠文字、鼠尾草綠生活區，少量暖黃、陶土橙與天空藍裝飾。
- 首屏文字／明亮照片分欄，拱形照片、紙片圖說與嫩芽線條；手機文字與照片上下排列。
- 首頁：品牌首屏、陪伴理念、孩子的一天、五校分校資訊、近期活動與最新消息、頁尾。詳細照片探索保留於各校頁面。
- 作品尚無素材，先以既有義華校園藝與學習照片呈現，未虚構孩子畫作或語錄。
- 原始三個入口檔備份於 `design/studio-original/`。此目錄為原始檔備份，非獨立可瀏覽網站。
- 沿用既有 LINE Seed TW 與系統內文字型。使用本機 HTTP 預覽，以避免 file URL 的字型 CORS 限制。

## 首屏主視覺（2026-09-14 審查後定案）
審查結論：版型（左對齊、手繪底線、中文手動斷行）沒問題，問題出在「先把素材壓暗再放白字」的深色影片 hero 反射，使真實校園照失去日光與品牌色。以下為修正後的規則。

- **遮罩只保護文字，不蓋整個畫面。** `.studio-hero::before` 改為三條各自對應一個可讀性需求的帶：頁首帶（頂部 96px 實心、200px 內淡出）、底部說明列帶（250px）、左側文字欄帶（0–34% 實心，72% 歸零）。全部走 `rgb(var(--ink) / a)`，不再出現 `rgb(0 0 0 / a)` 或 `#fff` 字面值。孩子的臉所在的右側維持全日光。
- **數值依實測照片亮度決定，不憑感覺。** 桌面白字對 `assets/hero.webp` 的平均對比：導覽 5.78、eyebrow 4.65、預約鈕 5.01、副標 6.08，全部過 4.5:1。改動遮罩後必須重量，勿只憑肉眼。
- **手機不做滿版壓字。** 2.49:1 的橫幅照片放進 0.46:1 的手機首屏，孩子的白色制服正好落在文字位置，實測副標對比只有 2.66，加厚遮罩會把照片壓死。改為：照片佔上方 `clamp(180px,100% - 500px,46%)` 的帶（`object-position:82% center`），下緣淡入 `--deep`；文案、影片說明列、底部列全部落在深綠面板上，白字對比 13.8:1。照片因此從放大 1.46 倍變成縮小 0.6 倍，可見範圍從原圖寬度 19% 提升到 46%，臉不再被裁。
- **主要行動只有一個主色。** hero 主鈕 `background:var(--yellow);color:var(--deep)`（9.65:1），文案「預約來校園走走」明說是預約；頁首「預約參觀」維持白框幽靈鈕當重複入口。焦點環 `outline-offset:5px` 落在按鈕外的深色底上（13.97:1），不壓在黃色上。
- **品牌色在 hero 至少留一筆**：`慢慢長大` 的手繪底線 `stroke:var(--yellow)`。其餘文字維持白色。
- **一屏只留兩句**：h1 與副標。已刪除 `.hero-note`（原「給孩子，一個喜歡上學的理由。」）。底部左側改放校區資訊「五所校園：三民 · 左營 · 鳥松 · 仁武」，同時回應比較型家長；不再限制 `max-width:14ch`。
- **狀態文字不得超前畫面。** `#media-caption` 預設「義華校 · 生活影像」，只有在 `video` 的 `playing` 事件後才改成影片文案；減少動態、省流量、慢速連線與載入失敗時都維持海報文案。自動播放除 `saveData` 外也擋 `effectiveType` 為 2g／3g。
- **hero 高度必須收進一屏**：`.studio-hero-grid` 的 `padding-block` 改 `clamp(28px,5vh,56px)`。1280×800 與 1440×900 實測皆為單屏，暫停鈕不再落在摺線下。
- **素材規格（待園方提供）**：海報原圖至少 2400px 寬；另需一張直式校園照供手機使用；影片至少 1920×1080，並另備直式版。首屏影片現為園方廣告片剪出的 `assets/hero-campus.mp4`（見下方 2026-09-15 節）。
- **已移除的死碼**：`.paper-sun`、`.hero-sprout`、`.studio-hero-image figcaption`（DOM 中皆不存在）。`.seed-dot`（eyebrow 前的裝飾圓點）依「圖示只出現在動作旁」規則移除。
- **打包器**：`package_preview.py` 原本只內嵌 `styles.css`，會讓 `preview.html` 完全失去工作室主題；已修正為同時內嵌 `studio.css`。

## 首屏字型、按鈕與校園影片（2026-09-15 定案）
- **標語字型**：維持 LINE Seed TW ExtraBold 800；尺寸 `clamp(2.5rem,1.55rem + 3.6vw,4.6rem)`（1440px 時 73.6px）、行高 1.24、字距 .01em，陰影改為柔一點的 `0 2px 28px` 加 `0 1px 2px`。句尾標點（，。）以 `.punct{margin-inline-start:-.24em}` 收進來：子集字型雖列有 `halt`／`palt`，實測在 Chrome 無效（「，」「。」仍是 1em 全形），所以用負邊距而不是 OpenType 特性。手繪底線只畫在「慢慢長大」四字下，句號在底線外。
- **hero 的字型例外**：eyebrow、兩顆主按鈕、影片控制鈕改用 LINE Seed TW Bold 700（.8125–.9375rem，字距 .04–.18em），讓首屏是同一個聲音；其他頁面的按鈕與內文仍是系統字。用字全在 `chars-bd.txt` 子集內，沒有新增字型檔。
- **副標**：`clamp(1.0625rem,.95rem + .45vw,1.25rem)`、行高 1.85、`color-mix(in oklch,var(--white) 90%,transparent)`，比標語退一層。
- **按鈕**：兩顆同高 56px（手機 54px、直向堆疊滿寬），圓角維持 2px。主鈕黃底深綠字、日曆圖示放在文字前（gap 10px），`text-shadow:none`；次要行動「看看孩子的一天」由文字連結改為白框幽靈鈕，與頁首「預約參觀」同一語言。hover 只在 `(hover:hover)` 下上浮 2px 加陰影，active 回落；transition 走 ease-out-quint。
- **2026-09-23 首屏按鈕比稿全數否決**：web/ 用 `?cta=` 試過 a 霧面深綠膠囊（同影片播放鈕語言）、b 米白實心膠囊＋黃色圓形向下箭頭、c 56px 細框圓形箭頭＋底線文字（無外框），三者皆加 Phosphor 向下箭頭；使用者看過後「都不用」，維持現行白框幽靈鈕，比稿碼已移除。勿再主動提這三個方向。
- **2026-09-23 下午 首屏拿掉「看看孩子的一天」按鈕**（取代上一條的「維持白框」）：web/ 首屏不再有任何按鈕，桌機（≥1001px）文字整段往下移 92px（`.studio-hero-copy{margin-top:92px}`＝原按鈕 36px 上距＋56px 高），文字底緣落在原按鈕底緣；窄螢幕文字在照片下方，不下移。手機只留「找校區 →」底線連結，改靠左對齊。hero 按鈕的死碼樣式與 `--hero-veil` 一併刪除；`.studio-actions` 容器保留（`useHomeReveal` 依賴它）。
- **2026-09-23 傍晚 首屏文字改回往上拉**（取代上一條的「往下移 92px」）：桌機改為 `.studio-hero-copy{margin-bottom:48px}`，文字塊中心落在視窗中心上方約 10px（光學中心）；grid 上有頁首 92px、下有 64px，單純置中會比視窗中心低。標語另以 `?copy=a|b|c|d` 比稿中，未定案。
- **2026-09-23 傍晚 首屏小標定案「常春藤幼兒園 · 陪高雄孩子近三十年」**：取代上午的「常春藤幼兒園」（原為「· 高雄五校」）。比過的 1997 年創立於高雄、三民／左營／鳥松／仁武、五所校園同一份用心、專業保育溫暖陪伴落選。小標走系統字不受子集限制；不放「30 週年」（未拍板）與「雙語／美語」字樣。正式站以後台首頁主視覺為準。
- **進場**：eyebrow → 標語 → 副標 → 按鈕依序上浮（.9s，間隔 120–160ms），底線在 .8s 後用 `stroke-dasharray` 畫出（1.1s）。所有基底樣式就是完成狀態，`prefers-reduced-motion` 時直接顯示，已實測（`animation:none` 下 opacity 1、dashoffset 0）。
- **影片**：`assets/hero-campus.mp4` 由園方廣告片剪出，`design/hero-video/build.sh` 可重剪：右上角校徽以 delogo 抹除、裁掉底部字幕帶成 1080×800、七個戶外鏡頭放慢 0.5× 並補幀、11.7 秒、2.5MB、無音軌。刻意不用 31.6–32.4 秒那顆（綠色 T 恤上的大字 VIETNAM 會搶過中文標語）。開頭是黃衣女孩的笑容（同時是海報幀 `hero-campus-still.webp`），結尾是另一個鏡頭，loop 接點只是一般剪接，不會在同一鏡頭內跳格。`object-position:50% 35%`（手機 `50% 40%`）。文案：海報「常春藤 · 校園生活影像」，`playing` 後「常春藤 · 校園生活影片」。
- **2026-09-22 Nuxt 採用清晰修復版**：使用者確認部署 `design/hero-video-restoration-20260922/` 的 11.7 秒短片。從新提供的廣告原檔製作，保留七鏡頭／半速／自然膚色，輕度去噪與銳化；揮手及跳躍兩段保留原始影格，避免光流補幀重影。桌機 1080×800／4.17 MB、手機 720×534／2.05 MB，使用內容雜湊網址及新版 `hero-campus-restored-v1-still.webp` 封面。衍生影片直接採用已確認的輸出，不再次壓縮；保留減少動態、省流量、慢速連線的靜態封面。原型 `assets/` 保持凍結。
- **遮罩新增第四條帶**：右下角 `radial-gradient(60% 48% at 100% 100%,rgb(var(--ink) / .55) 0 30%,rgb(var(--ink) / 0))`，只服務影片說明列與控制鈕。離線以 12 幀試算（`design/hero-video/` 同一套遮罩模型）：說明列最差幀 3.59 → 6.66、控制鈕 4.08 → 8.11；臉部所在的中央區不受影響（2.82 → 2.84）。
- **對比實測（1440×900，遮罩後、12 幀最差值）**：標語 7.22、副標 6.86、eyebrow 7.68、幽靈鈕 5.83、導覽 6.37、說明列 6.66、控制鈕 8.14，全部過 4.5:1；海報幀更高（標語 11.9）。手機文案仍在深綠面板上，未變。
- **素材限制**：來源是 1080×1080 社群版廣告，字幕與浮水印都燒在畫面裡，桌面放大到 1440 寬後略軟。正式版請園方提供無字幕、無浮水印的 1920×1080 原始檔，改 `build.sh` 的鏡頭秒數重剪即可。

## 頁首預約鈕形狀（2026-09-15 晚間更新）
- 依使用者提供的 APPLY NOW 參考圖，`.header-book` 改為 10px 圓角長方形，桌面左右 padding 48px。
- 1100px 以下沿用原有響應式留白，手機最低觸控高度 44px；首頁白框與內頁綠底沿用既有色彩設定。

## 關於常春藤區塊（2026-09-15 定案）
- 首頁 hero 之後的區塊由「我們相信」改為「關於常春藤」：左邊文字、右邊照片，兩欄各 1fr、間距 96px、垂直置中；文字欄 `max-width:560px`。裝飾葉子 `.cutout-leaf` 與遠離標題的 eyebrow 三欄版型一併移除。eyebrow 改為「關於常春藤」，與導覽名稱一致。
- 背景依使用者 2026-09-15 晚間要求改為純白 `#fff`，只作用於 `.studio-about`；保留照片圖說的米色紙片。
- 文案依園方本次提供的在地口碑、歐式城堡、愛與關懷、閱讀、自理、個別差異及終身學習理念重寫。主標「在地近三十年，把每個孩子，放在心上。」；因資料同時提及 27 年與 30 年，採「近三十年」，未加入創辦年份。內文以「生命影響生命」串起教育初心，結尾保留「認識五所校園」。
- 照片 `assets/about-curious.webp`（760×570）從廣告片 16.0 秒抽幀、避開右上校徽與底部字幕裁成 4:3；圖說紙片「常春藤的孩子 · 校園片刻」壓在照片左下角（`--cream` 底、負 16px 出血）。正式版由園方換成選定的照片，換檔即可。
- 2026-09-16 依使用者指定，移除「關懷／閱讀／自理」整塊與頁尾對應的「教育理念」入口；關於常春藤的介紹與照片之後直接接續「孩子的一天」。
- 手機（≤760px）文字在上、照片在下，照片改 3:2，圖說紙片收進照片內側左下。
- 方向畫布：`design/believe-directions/`（gen.py 產生），第一頁為採用版，第二頁保留未採用的方向 B（一條發芽的線）與方向 C（三個片刻）。
- 本次文案更新驗證：375／390／768／1024／1440px 無水平溢出或文字裁切；區塊背景實測 `rgb(255, 255, 255)`，純白底上的文字最低對比 6.30:1。已從 LINE 官方相同 1.400 版字型補齊 Bold 子集，既有字元字寬一致；`preview.html` 同步。

## 首屏字體方向 A（2026-09-15 晚間選定）
- 使用者在四款字體比較後選定 A「清爽溫暖」，取代本文件前述首頁主標 800 字重、最大 73.6px 的設定。
- 首頁主標使用既有 LINE Seed TW Bold 700 子集；桌面 `clamp(2.75rem,1.5rem + 3vw,3.75rem)`（1440px 時 60px），手機 `clamp(2rem,1rem + 5.5vw,2.125rem)`（390px 時 34px）。行高 1.32、字距 .015em，使用真實字重並以 `font-size-adjust:from-font` 調整替補字型。
- 副標桌面 1.125rem（18px）、手機 1rem（16px），沿用 500 字重與 1.85 行高；文字改為不透明 `var(--white)`，維持較亮影片畫面的清楚度。
- 字型比較畫布保留於 `design/hero-type-options/index.html`，包含 A–D、選定前原版、桌面／手機及字級微調。
- 驗證：1440／768／390／375px 無水平溢出，主行動可見，固定頁首與導覽跳轉正常，`preview.html` 同步。1440×900、捲動位置 0 的影片等距取樣 12 幀，隱藏文字後對每段文字矩形內的背景逐像素估算，未計文字陰影的保守最低對比為主標 4.67、副標 4.66；這是取樣結果，不代表逐幀完整掃描。

## 五校地圖與社群（2026-09-15 定案，方向 B；首頁部分已於 2026-09-16 被下節取代，各校內頁的 Google 嵌入規則仍有效）
- 五校卡片、各校「交通與聯絡」都加了 Google 地圖與 LINE／Facebook。方向比較與資料來源見 `design/campus-directions/README.md`；正式規則如下。
- 卡片動作列固定順序：電話（既有 `.campus-meta`）→ LINE 圖示 → Facebook 圖示 → 地圖按鈕（`.campus-social.map`，`aria-expanded` 反映抽屜開關）。沒有 LINE 帳號的校區用虛線圓框＋`is-pending`，不留白也不冒用其他校區帳號。
- 地圖抽屜（`#campus-map-drawer`）是 `.campus-grid` 裡唯一一個 `grid-column:1/-1` 的元素：**桌機／平板固定 `order:99`（永遠排在整排最後），只有 ≤760px 單欄版才用 `order` 插到被點的卡片正下方**。這條規則不能反過來——若桌機也用「照片片中間插入」的 order，CSS Grid 的 auto-flow 會被強制斷行，後面的卡片會被推到下一列、擠成單欄（實測過，5 欄與 3 欄都會壞）。
- Google 地圖一律用免金鑰的單點嵌入（`maps?q=地址&z=16&output=embed`）。iframe 用 `data-src` + `IntersectionObserver`（`rootMargin:160px`）延後到進視口附近才給 `src`；Google 的 embed 載入當下會把捲動位置拉去對齊地圖，畫面外載入會整頁跳走。`render()` 每次換頁都呼叫 `armMapFrames(main)`，抽屜另外在展開當下呼叫一次。
- 校區資料新增 `line`（字串或 `null`）、`facebook`、`fbNote` 三個欄位；`campusSocial()`／`campusPills()`／`campusMapFrame()` 三個共用函式分別產生卡片圖示、抽屜與內頁的大按鈕、地圖區塊。

## 五校入口重排（2026-09-16 中午，取代上節的首頁卡片與地圖抽屜；首頁目錄版已於同日下午被下節「五校分區輪播」取代，社群連結與示意地圖的規則仍有效）
- 首頁五校入口只留三層：4:3 照片 → 校名（LINE Seed 700、1.375rem）→ 地區（.9375rem、`--muted`）。整個入口是一個 `<a class="campus-entry">` 連到校區頁，照片 `alt=""`，連結可及名稱＝校名＋地區；校名後不加箭頭（↗ 仍只給外部連結）。滑鼠移入只讓校名出現 2px 綠底線，不浮起、不放大；焦點沿用全站 3px 外框。
- 照片統一 `aspect-ratio:4/3`，裁切位置逐校用 `--photo-pos`（資料欄位 `photoPos`）：義華 `58% center`、國際 `48% center`、其餘置中。不加白框、陰影或大圓角（2px）。
- 內容寬沿用 `.container` 1280px，五欄 `repeat(5,minmax(0,1fr))`、gap 24px；≤1100px 縮字不減欄；≤760px 改五列清單（112×84 縮圖、列間 1px `--line`、列高 ≥44px），不做輪播。
- 「校區位置與聯絡資訊」常駐在入口下方（`.campus-contact`，上方一條 `--line` 分隔），沒有收合／展開列、沒有關閉叉叉：左 1/3 為校區 tab（`role=tablist`，已選＝700＋2px 綠底線，方向鍵切換、Home／End、循環）→ 地址與電話 → 唯一實底鈕 `.button.primary`「預約參觀Ｘ校」→ LINE／Facebook；右 2/3 為五校示意地圖，靠欄距與一條垂直細線分隔，不用白卡片。
- 社群連結 `.campus-link`：36px 圓形細框徽章（`--line` 框、`--paper` 底、`--green` 圖示，Simple Icons 官方標誌）＋標題列（含外連 ↗）＋一行小字說明；hover 徽章反白成綠底。無 LINE 的校區顯示「LINE 官方帳號／Ｘ校帳號待園方提供」（虛線徽章、不可點），Facebook 指機構粉專並在小字註明。同一元件也用在各校內頁「交通與聯絡」，原 `.campus-pill`／`.campus-social` 已移除。
- 地圖是 SVG 示意圖（`campusMap()`）：行政區邊界與五校座標放在 `app.js` 的 `CAMPUS_MAP`（由 `design/campus-directions/gen-map.py` 產生、只留視窗內 14 區），viewBox `80 150 480 270`＋`preserveAspectRatio=slice`，桌機 460px 高、手機 260px 共用同一個 viewBox。每校同時畫小圓點與大圖釘＋校名牌，`is-selected` 切換並把選中的 `<g>` 移到最後蓋在上層；點圖釘也能選校（焦點回到對應 tab）。右上角「規劃路線 ↗」開 Google 導航。首頁不再載入 Google iframe；各校內頁仍用單點嵌入。

## 五校分區輪播（2026-09-16 下午定案，取代上節的首頁五校目錄）
- 使用者在畫布比較後先選 A3，15:34 改走 **A2 版面＋單一長橢圓軌道**（本節已依最終版更新）。首頁「五校介紹」現為：標題、引言置中（`.section-heading.is-centered`，引言不換行、`max-width:32em`）→ 「← 選校軌道 →」置中（15:38 依使用者要求拿掉地區小標與分組線，五校不分區）→ 左大照片、右欄先 240px 示意地圖再校名地址預約社群；一次只看一校，每 6 秒自動循環五校。
- 選校軌道 `.campus-picker`（flex 置中、垂直置中，左右各一顆 44px 圓形上一校／下一校 `.campus-picker-nav`）包 `.campus-track`（淡灰米底 `oklch(93% .014 100)`、6px 內距、999px 圓角）放五個等寬 `.campus-seg`（44px 高、500 字重）；已選＝實心 `--green` 膠囊、`--paper` 字、700；hover 只轉 `--leaf`；焦點外框縮進軌道（`outline-offset:-3px`）。不放地區小標、不畫分組線（曾有，15:38 移除）。桌機軌道固定 652px，手機滿版、五段各約 67px、校名 .875rem 仍放得下。
- 舞台 `.campus-stage`：桌機 1.3fr／1fr、gap 48、左右同高；左 `figure` 的照片向下延伸至右欄底部（`object-fit:cover`、`--photo-pos` 逐校裁切，圓角 24px、手機 16px），移除照片下方的介紹句；手機照片維持 4:3；同一組上一校／下一校按鈕（`.campus-stage-btn`，首尾循環、不停用）桌機放在軌道兩側，≤760px 改顯示在照片下方右側（`.campus-stage-nav`），兩組同時存在於 DOM、以 CSS 切換。右欄依序：240px 示意地圖（`.campus-map-stage`，圓角 16px、完整 viewBox `80 150 480 270`）→ 校名（1.75rem）＋「高雄 · 地區」→ 所在地／參觀專線 → `.button.primary`「預約參觀Ｘ校」＋ `.text-link`「認識Ｘ校」（校區頁入口，輪播版必須有）→ LINE／Facebook 徽章（上節規則）。
- 行為（`setupCampusShowcase()`）：軌道是 `role=tablist`，←／→／Home／End 循環切換、Tab 只停一點；上一／下一校、點地圖圖釘（焦點回到對應段）都走同一個 `select()`。切換時先預載新照片，`.is-switching` 讓照片、資訊 200ms 淡出（ease-out-quint），載入完成後一起換再淡入；快速連點以 `switchId` 丟棄過時的切換；`prefers-reduced-motion` 直接換。手動切換後 `#campus-live`（`.sr-only`、`aria-live=polite`）播報「目前顯示Ｘ校，高雄Ｘ區」，`aria-labelledby` 同步。
- 自動輪播（2026-09-16）：依使用者要求移除暫停／播放鈕及其佔位。每 6 秒循環；滑鼠停留、舞台可見比例低於 20% 或頁面進入背景時清除計時器，恢復後重新計滿 6 秒。手動選校後重新計時；鍵盤焦點位於區塊內或觸控按住時暫停，焦點移出或觸控結束後恢復。滑鼠點選造成的焦點不會讓輪播永久停止；減少動態效果時暫停。自動切換不播報、不移動焦點；換頁時清除計時器、observer 與全域監聽。
- 照片路徑集中在 `photoSrc(name)`，`img()` 與輪播換圖都用它；`package_preview.py` 改為只替換這一行（找不到會直接 assert 失敗），不再靠字串比對 `src="assets/..."`。
- 素材待園方：目前每校只有一張 640px 寬的外觀照，放到 696px 會軟；正式版每校至少 1400px 寬的 4:3 外觀照，義華可另選一張以孩子活動為主的照片。

## 首頁頁首線條版恢復（2026-09-15 晚間）
- 依使用者要求，恢復 `design/hero-type-options/index.html` 21:01 快照中的首頁頁首：透明底、白色字標與導覽、彩色校徽、10px 圓角白色細框預約鈕；捲動後使用既有深綠底。Logo 沿用 `assets/logo.png`，以 `#logo-white` 去除白底並轉白字標，再由 `.brand::after` 與 `#logo-colour-cutout` 還原左側校徽色塊，作用域限於 `body:has(.studio-hero)`。
- 本次實際重現的頁首失效原因是圖示 sprite 缺少 `</svg>`：瀏覽器把 `.header` 解析為 SVG 命名空間元素，固定定位與顏色繼承因此失效。已補回結尾；兩個 filter 定義放在獨立、零尺寸的 SVG 中，不移除 sprite 的結尾標籤。
- `package_preview.py` 同時內嵌 Logo 的 CSS 背景圖片，使單檔 `preview.html` 的校徽疊色與首頁一致。


## 最新消息：採用 A 版（2026-09-16）

園方選定 `design/news-directions/` 的 A「活動＋新聞」。首頁於分校資訊後、FAQ 前呈現左側三個活動日期卡及右側三則圖片消息；手機依 DOM 順序先讀活動，再讀縮圖消息。標題沿用 LINE Seed TW，底色米白、活動卡淡綠／米色，不使用自動輪播。（「不使用自動輪播」已由 2026-09-23 最新消息 C「原地換片」取代，見該節；手機仍不輪播。）

消息卡以 article + h3 + button 表達層級，按鈕擴展點擊範圍至整張卡片；原生 dialog 提供完整清單、文章／活動詳情、返回清單、Escape 及焦點還原。路由切換時關閉視窗。消息和日期暫為範例，首頁與詳情均明示；不暗示活動已公告或開放報名。

首頁連結為 `#/home/latest-news`。所有圖片仍走 `photoSrc`，維持 `preview.html` 的單檔離線預覽。整合前快照位於 `versions/before-news-a-20260916-222759/`。

### Nuxt 版紙頁覆疊 E（2026-09-22，取代 9/21 水平掃色）

首頁活動卡與「所有活動／所有最新消息」入口採無圖示設計，移除斜箭頭；清單入口保留文字細底線，活動卡保留 hover 外框及鍵盤焦點。此調整僅作用於 Nuxt 首頁區塊。

使用者從 `design/news-transition-explore-20260922/` 選定 E「紙頁覆疊」，正式整合至 `web/`。以 TrueKind 分層揭露的研究延伸為紙頁：現行圓角分校輪播輕微後退（1→.935），消息紙頁隨自然捲動由下往上覆入、尺度 .92→1、上方圓角 88→0px（手機 40→0px）。紙頁後半段從暖白 `--news-paper-base:#faf7ef` 整面轉成霧藍 `--news-paper-blue:#dce7eb`，往上捲反向還原。保留活動＋新聞版型、深綠文字與既有照片，三張活動卡固定暖黃、嫩綠、霧青；不再疊加舊的逐卡／文字水平掃色。

`HomeNewsTransition.vue` 放在 DayExperience 的 slot，包住單份 CampusBoard 與 NewsDialog；承接原本 day→campus 的負邊距，分校維持 sticky，消息走正常文件流，不增加空白捲動軌道。CSS 註冊 `--news-paper-progress` 由紙頁的原生 view timeline（`entry 0% → entry 100%`）驅動；未支援則採相同公式的 passive scroll＋rAF 備援。沿用共用 `motionViewport`，手機同寬高度變化維持閱讀高度。前段完全被蓋住時設 inert 並裁掉隱藏層；反向捲動解除。手機消息頂部保留 88px，避開膠囊導覽。減少動態／高對比回到正常排列，暖白靜態背景；dialog、整張文章點擊範圍、Escape 與焦點還原保留，卸載清除 observer／listener／rAF。原型維持凍結。

### 首頁活動卡暖色 hover（2026-09-22）

依使用者截圖，首頁活動卡在滑鼠移入時，由上往下填入暖色；移開後向上收回。同日使用者從六組配色中選定 A「蜜糖日光」，三卡依序採金黃 `--hn-hover-gold:#f6cd68`、蜂蜜 `--hn-hover-honey:#eabc74`、燕麥 `--hn-hover-oat:#f5dda3`，取代初版蜜桃橘／杏桃金／玫瑰奶茶。靜止底色沿用暖黃／嫩綠／霧青，深綠文字、尺寸與圓角不變。

填色為獨立 pseudo-element，以 560ms 的 transform transition 推進，不縮放文字、不推動版面；只在支援 hover 的精準游標裝置播放，鍵盤 focus-visible 也可觸發。減少動態直接換色，高對比隱藏裝飾填色層。樣式限於 `.home-news .hn-event`，活動 dialog 與紙頁捲動轉場沿用既有行為；實作參考 [web.dev CSS transitions](https://web.dev/learn/css/transitions)。


## 首頁移除 FAQ 與預約橫幅（2026-09-16）

園方要求暫時拿掉首頁「參觀前，你可能想知道」及「親自走一趟」兩段。首頁改為最新消息後直接接頁尾，移除指向首頁 FAQ 的導覽；各分校頁仍使用共用 FAQ 與預約橫幅，頁首預約入口與預約頁保留。


## 關於常春藤 B2 與背景停留（2026-09-17，取代前述關於區塊版型）

- 採用 `design/b-layout-extensions/b2.html`：左欄 SINCE 1997 →「把每個孩子，放在心上。」→ 一段加長介紹 → 認識五所校園；右欄大幅孩子照片與左下方探索照片重疊，保留米白相框。移除 01 編號、關於常春藤小標、Growing together 與標題底線。
- 淡綠 `#c5ded0` 滿寬背景與淺色「關於／常春藤」大字放在 `.belief-backdrop`，與 `.belief-content` 疊於同一 CSS Grid 格。Backdrop 使用 sticky，頂端對齊固定導覽的 92px／80px／78px，填滿其下的視窗。內容自然捲動，不攔截 wheel，不新增計時器。
- `.belief-content` 的 `--belief-entry-space` 為桌面 `clamp(180px,32svh,360px)`，平板 `clamp(180px,28svh,280px)`，手機 `clamp(160px,24svh,240px)`；這段額外背景讓圖片和文字較晚進場。調整背景上方留白只改此變數，不改原 hero 揭幕的距離或進度。
- 內容寬度最高 1440px，桌面 1.05fr／1fr，900px 以下上下排列。主照片用 about-curious、小照片用 learning，皆透過既有 img／photoSrc 打包；手機仍保留錯落雙照片。裝飾字 aria-hidden、不接收游標事件；減少動態時背景回到普通流，forced-colors 隱藏裝飾字。
- Chromium 桌面／平板／手機共 10 種尺寸檢查無水平溢出，固定背景與文字的實際捲動位移分別為 0／-120px；原生 reveal、JavaScript fallback、減少動態、導覽錨點及校區連結均通過。Safari／Firefox 仍待實機驗證。

## 分校資訊帶 B「校名主場」（2026-09-21 定案）

使用者選定 prototype/design/campus-contact-three-20260921 的 B。僅更新 Nuxt CampusBoard 資訊帶，取代 e3 底板的一列四欄：大校名＋地區／英文副標、聯絡資訊＋社群、暖黃膠囊預約。桌機三欄、平板行動移下、手機單欄。照片及切校行為不變；缺 LINE 保留待補；所有預約仍走 BookingCta。樣式限定 campus-contact-b，避免影響分校內頁及消息換色。

同日依使用者截圖微調：校名不加句號；校園位置與參觀專線顯示既有圖釘／電話 icon，LINE／Facebook 顯示既有品牌 icon 並保留文字。地址本身帶底線與外連箭頭，另開 Google Maps；移除獨立的「查看位置與路線」及其底線。地址與社群保留至少 44px 點擊高度，鍵盤焦點使用暖黃外框。

## 分校滿版 B「左右分景」（2026-09-21 方向選定，線稿待選）

使用者在 `design/campus-fullscreen-20260921/` 的 A／B／C 三款中選定 B：左側暖白校名與聯絡欄、右側滿高校園照片，向下捲動切校，手機重排成上下構圖。接續的建築線稿位置仍在 `design/campus-b-lineart-20260921/` 比較 B1 左欄底景、B2 校名標記、B3 照片下緣獨立圖帶，尚未正式整合至 Nuxt 首頁。

三款沿用五校各自的既有線稿；每次切校同步更新照片、校名、聯絡資訊與線稿。裝飾不穿過文字或操作項，手機另留線稿空間。保留無句號校名、位置／電話／社群 icon，以及地址本身開啟 Google Maps 的動作。

同日追加探索：使用者希望線稿「融入背景、補足圖片」，新增 `design/campus-b-blend-20260921/` B4 建築融景。讓米白底上的線條接入實景，手機改為照片下緣融入線稿；沿用現有素材並調整各校接縫附近的比例與位置。此為融合方向 mock-up，尚未選定接縫細節，也未整合至 Nuxt 首頁。

同日晚間：使用者回到無線稿 B，指定校名移至上方水平並排，切校呈現像 IG 限時動態。`design/campus-fullscreen-20260921/stage.html?direction=b` 更新為五段進度與五校校名：完成段深綠、目前段暖黃推進、未到段淡色；照片短淡入，保留捲動／點選／鍵盤控制。手機同樣一列，減少動態維持普通五校列表與 sticky 導覽。此版仍為 mock-up，未整合至 Nuxt 首頁。

## 分校 B 自動限時動態（2026-09-21 晚間定案並整合 Nuxt）

使用者接續指示「走 B，然後要自動跑」。首頁 `CampusBoard.vue` 採無線稿 B：左側米白資訊、右側滿高照片、上方水平五校與分段進度；手機上下重排，五校仍一列。進度改由時間推進，每校 6 秒、末校回首校，照片短淡入；區塊使用一般頁面捲動，不保留 mock-up 的五屏捲動距離。

點校名或方向鍵／Home／End 可直接切校並重算六秒。顯示暫停／播放控制；閱讀與校名區的滑鼠停留、內容鍵盤焦點、分頁隱藏、低於 35% 可見時暫停，返回後接續剩餘時間。減少動態預設關閉自動播放，仍可手動選校或主動開始；只有一個已發布校區時不輪播。可見性判斷取同批 IntersectionObserver 的最後一筆，避免快速捲動／縮放後停在舊的隱藏狀態。

保留校名無句號、位置／電話／LINE／Facebook 圖示、地址直接開地圖、各校既有社群與預約設定，以及同步協作新增的分校內頁連結。未改 CMS 發布資料、未部署。Chrome 四尺寸五校 20 狀態、自動循環、暫停續播、鍵盤、離屏與減少動態皆通過；計時與預約動作 15 項測試、Nuxt typecheck 通過。五校 fixture 預覽使用獨立暫存副本的 3012；3010 當前 release 只有明華校。


### 膠囊進度與版面細修（2026-09-21 21:11，取代上方分段跑條）

使用者提供膠囊進度＋圓形播放鍵參考圖，已套用至 B 版。校名維持上方水平一列，只用字重／底線標示目前校區；倒數移至左欄底部的灰米色膠囊，當前校區為展開的填色長條，其餘四校為圓點。每個圓點都是至少 44px 的按鈕，可滑鼠、觸控或鍵盤選校；旁邊放獨立播放／暫停圓鈕，使用既有 Phosphor 圖示。底下顯示播放說明與目前序號。每校 6 秒、末校回首校、離屏／隱藏分頁／減少動態行為延續；滑鼠或觸控選校後重新倒數，只有鍵盤可見焦點會暫停閱讀。

左欄聯絡資料改成標籤與內容對齊的兩列，分校入口為描邊膠囊；共用 BookingCta 回傳暫停時，以左側細金線的小字訊息呈現，開放預約仍保留暖黃按鈕。右側照片維持滿版、移除額外放大裁切。手機採上下構圖，控制列置於資訊下方；窄平板增加左欄比例並留出浮動頁首空間。無線稿、無校名句號，地址直接開地圖與社群 icon 均保留。

驗證：Chrome 1440／1024／768／390／320px × 五校 25 狀態、點選／鍵盤／手機觸控、6 秒循環、暫停續播、離屏、減少動態均通過；另以瀏覽器攔截回應驗證開放預約 CTA 不壓到控制列，未改動後台資料。截圖及結果在 `output/playwright/campus-capsule/`。


## 官網後台手機清單與編輯保護（2026-09-22）

- 延續後台淺色、系統字及深綠操作色。時段、通知、使用者與操作紀錄在 720px 以下改成直向清單，資訊、狀態、動作放在同一筆內，手機主要新增動作滿寬。桌機沿用表格。
- 清單篩選有明確標籤；使用者可搜尋 Email／角色／校區與啟用狀態，操作紀錄搜尋限定「已載入的紀錄」。載入中不顯示成空清單；失敗有持續可見的原因與重新載入入口。只採用最後一次讀取回應，避免舊校區資料覆蓋新校區。
- 預約設定與全站設定共用未儲存攔截：取消離開保留輸入；確認放棄才切校／離頁；處理中鎖住相關操作。預約設定版本衝突保留編輯並明示需載入新版，不自動丟棄內容。
- 通知批次已讀顯示進度，部分失敗保留未讀並可再試；時段名額旁明示「調整後立即儲存」。使用者清單重新整理與帳號寫入互斥，對話框處理中不可意外關閉。
- 校園探索使用 Pointer Events 與 pointer capture，觸控圖釘至少 44×44px；方向鍵／Shift 微調保持可用。切換場景與離頁時移除拖曳監聽。
- 個資清理在改天數、重查失敗或無法確認執行結果後清除舊報告，需重新檢查才能再次執行。此輪只驗證合成 API 回應，不執行真實清理。

## 官網後台操作一致性（2026-09-21 第二輪）

- 後台沿用淺色、系統字、品牌深綠操作與淡綠中性色，與公開官網的影像及動效設計分開。共用 token 定義在 `admin/src/style.css`。
- 側欄沿用 `NAV_GROUPS` 作為導覽與權限來源，加入功能搜尋與分組收合；目前頁面的組別自動展開。900px 以下使用具有焦點管理、Esc 關閉與背景捲動鎖定的 Element Plus 抽屜。
- 總覽以實際摘要、可操作的待辦提醒和工作捷徑安排層級；不把「未發布草稿」數字任意連到某一篇內容。「今日參觀」的入口明示是全部已確認案件。
- 內容編輯清楚說明儲存草稿與發布的差別；載入時只呈現骨架，處理中鎖住表單及重複操作。取消離開必須保留原頁內容、網址與分頁標題。
- 案件列表在手機改為直向清單，家長名稱可開明細、電話可撥打。篩選清空、沒有結果及讀取失敗都有下一步；快速切換條件只接受最新回應。
- 素材搜尋涵蓋檔名及替代文字，提供清除篩選與讀取重試。對話框左右至少保留 16px，手機控制項至少 44px 高；文字輸入使用 16px 避免手機自動放大。


## 手機捲動穩定與紙張載入（2026-09-22）

- Nuxt 首頁的 Hero、關於／日常兩道簾幕、大字接力及拍立得間距共用 `--reveal-height`／`--motion-vh`。觸控裝置同寬的高度變動保留本次閱讀基準，避免網址列或鍵盤改高時切換動畫模式、推移內容；改寬／旋轉、內容增高及重新進入首頁仍重新量測。原生 Hero timeline 也使用相同的有效高度。
- 保留原有向上揭幕與浮水印接力。幾何位置在尺寸變動時快取，捲動中只算進度；日常標題透明度直接跟隨捲動，不再額外追趕 200ms。
- 手機先顯示可操作的 CSS 拍立得，停止滑動至少 200ms 後才逐張初始化畫面內的 WebGL 紙張；顯影、掀角、偷看或翻面進行中也先等待。離開畫面取消排程，頁面卸載清除排程與資源。初始化讀取最新正反面與顯影狀態，避免接手時重播動畫或翻回正面。
- 保留六張內容、照片、紙膠帶、44px 折角、手機 900ms 顯影、首張偷看及柔軟翻面光影。紙底／文字快取，顯影只重畫照片區，掀角只重畫角落；桌機仍在接近時預載，減少動態、節流設定或無 WebGL 沿用既有回退政策。
- 僅更新 `web/`；根目錄 vanilla 原型保持凍結。Chrome 手機模擬與 CPU 降速量測不等同 iPhone Safari 實機驗收。

## 手機閱讀與預約動線（2026-09-21 第一輪）

- 依手機 UX 檢視後的使用者授權，保留品牌、完整六張拍立得與當前分校輪播。手機首屏新增「找校區」文字捷徑，直接至首頁分校區。
- ≤760px 關於介紹先顯示已發布文案第一個完整句子，其餘可透過「閱讀完整介紹／收合介紹」展開；桌機仍顯示完整文字。摘要不另存 CMS，不複製或重寫園方文案；沒有句尾標點時保留全文。展開／收合沿用 ResizeObserver 重算簾幕幾何。
- 手機關於進場留白改為 `clamp(64px,10svh,96px)`；seam=2 的 belief 距離改為 55% 視窗高、尾留白 30svh 加既有字級補償；首張拍立得相對同一距離抵銷 32svh。卡間距 32px、底部 padding 64px；桌機動效距離不變。
- 通用預約頁不預選首校，所有模式共用常駐的原生校區選擇器。指定校區路由保留帶入值。暫停或外部聯絡模式顯示狀態與該校電話／既有 LINE，不冒用其他校區；未選校或讀取中不顯示可送出表單。送出時不能切校，切校保留個人輸入但清除過時錯誤／時段。
- `BookingCta` 的不可操作狀態不再帶 buttonClass，使用一般狀態文字。各區塊可依背景配置文字顏色，不能把暫停提示誤裝成可操作的預約鈕。
- 分校大校名與照片皆連到當前校區內頁；非當前照片 inert 且不接收點按。已有「認識此校」入口、輪播控制與進度樣式沿用當前最新裁定。
- 本輪未更動預約 API、DB、CMS、部署及正式訊息；驗證在獨立五校 fixture 預覽與攔截的表單請求中完成。

### 手機第二輪：顯影、小字與殘留空白（2026-09-21 晚間）

- ≤760px 拍立得顯影縮到 1 秒內：CSS 版 `.print-photo`／`.print-figure::after` 0.9s、時間戳 0.8s 延遲 0.5s；WebGL 版 `paperPrints.ts` 依同一個 760px 查詢改用 `DEVELOP_MS_MOBILE = 900`。桌機仍是 3.2s 慢顯影。理由：手機一滑就過，3 秒顯影讓截圖與快速瀏覽時照片一直是白的。兩處時長要一起改。
- 手機拍立得 kicker、翻面提示提高到 12px，相片角落說明 11px；不動故事正文。
- seam=2 手機浮水印透明度降到 .28，「閱讀完整介紹」按鈕帶 `--belief-bg` 底色並提到浮水印之上，避免大字疊在按鈕文字上。
- 膠囊選單五校連結高度 36→44px。
- 分校內頁「交通與聯絡」在手機原本為 `.campus-art-building` 線稿預留 128px 底部空間，Nuxt 版沒有這個元素，已移除。

## 分校線稿按鈕框線（2026-09-22 比稿）

使用者已否決 `design/campus-lineart-placement-20260922/` A 的「沿各校建築外緣描邊」版本；勿再主動提同方向。後續新增 `design/campus-tab-framing-20260922/` 比較 A 留白短線、B 校名膠囊、C 輕框卡片，皆保留原線稿且未選定。此裁定僅針對框線比稿，正式 Nuxt 尚未整合線稿。

## 分校線稿 A「留白短線」（2026-09-22 定案並整合 Nuxt）

使用者在三個新方向中選定 A。正式 `CampusBoard.vue` 採五校線稿＋下方校名，選中校名下方加短底線；桌機 25 × 2px、手機 20 × 2px，移入為淡灰線。移除選校列上下長線及矩形底色。桌機線稿 120 × 80px、手機 60 × 40px，極窄螢幕 48 × 34px，維持一列五校與置中標題。沿用原始線稿的 CSS 混色及響應式素材，圖片為裝飾，校名保留完整可存取名稱。

同日依使用者截圖追加放大：桌機線稿寬度改為 160px，維持 3:2 圖框比例並依按鈕可用寬度縮放；校名字級改為 1.0625rem（預設 17px）、選校列寬度上限改為 1040px。手機尺寸、線稿色調與短底線沿用上述定案。

2026-09-23 依使用者要求，hover 線稿亮度由 `.72` 小幅降至 `.68`，輪廓稍微加深，濾鏡以 `.2s` 過渡；一般與僅選中狀態維持原色調。

同日晚間依使用者要求，桌機 hover 時線稿轉為彩色：各校實景照片經 TPS 對位後做成只有顏色的淡彩層（`campus-line-art-<校區>-colour.webp`），以 multiply 疊在原線稿上 `.35s` 淡入，線條仍由原線稿提供。照片裡的文字、號誌與行人不帶進淡彩，天空只留很淡一層，延續線稿不含文字的原則。只在 `hover:hover` 裝置顯示，觸控不載入；選中狀態不變彩色。產生方式見 `design/campus-tab-colour-20260923/`。

保留現有四秒自動輪播、暫停、鍵盤、觸控、離屏／減少動態機制。既有建築外緣描框仍為已否決方案，校名膠囊與輕框卡片不套用。前台已整合，尚未部署。


## 2026-09-22 選單 B 直式目錄與毛玻璃定案

使用者選定三款導覽提案的 B，並指定頂端採深綠「探索常春藤」加水平細線。展開卡片改為米白直式目錄，保留雙語上下排、每列序號與向右箭頭；五校捷徑附行政區；依同日最新修正，下方先設分校電話與 IG／FB／YouTube／LINE，再以米白機構社群收尾。此裁定覆蓋 2026-09-21 選單卡的墨綠毛玻璃規則；膠囊本體維持原設定。

同日依使用者「優化顏色文字大小比例、樣式」精修：採暖白底與墨綠文字，深綠限於頂部標題帶；分校電話／社群改為內縮的淺鼠尾草綠區、淡金電話圓標。五校使用單一淡色底列，社群移除厚框及重複的「前往」。主導覽桌機 20px／手機 18px、校名 14px、電話 22px、輔助字 12px；所有有效連結維持至少 44×44px。「待提供」保留文字且不可點擊。字級使用 rem；390px 下 200% 文字放大與六尺寸操作驗證通過，實測文字對比最低 5.55:1。最新視覺以 `output/playwright/menu-polish-20260922/menu-desktop.png` 為準。

**同日最新修正：使用者明確不要墨綠毛玻璃，改為一般淺色毛玻璃。** B 選單保留直式目錄、精修字級、五校一列，以及分校在上／機構在下的順序；採中性乳白半透明底、深灰主字與次字，標題帶、五校底列、聯絡區及電話圖示一起去除綠色染色。面板使用 72% 乳白底、24px blur、1.1 saturation 與淡亮邊框。只在支援 backdrop-filter 時啟用；減少透明度、不支援濾鏡、強制色彩模式皆有實色備援。此段取代前述實色與墨綠玻璃色彩設定，尺寸、內容與既有互動不變。最新截圖及驗證在 `output/playwright/menu-neutral-glass-20260922/`。

同日追加：依使用者指定，分校社群列的有效連結只呈現平台圖示與名稱，移除 FB／LINE 旁的 ↗；未知帳號仍顯示「待提供」。新視窗的讀屏提示與實際連結保留。

機構社群先使用 repo 原有 Facebook `https://www.facebook.com/ivykid`，資料放 `siteMeta.socialLinks`。未取得其他機構社群網址前不猜填、不用義華帳號代填。外連另開新分頁並提供讀屏提示。矮螢幕以選單內捲動保留所有入口，Escape 返回原開關；主導覽使用 Nuxt 首頁錨點。


## 預約校園參觀 A（2026-09-22 定案並整合 Nuxt）

- 使用者選定 A，正式 `VisitForm.vue` 採桌機墨綠迎賓區＋校園照片／右側兩步流程。校區以原生 radio 的整列照片、校名、行政區、地址呈現；選中有框線與勾選。此決定取代 2026-09-21 的「常駐原生下拉」外觀，保留未預選、即時校區設定及切校保留輸入規則。
- 通用 `/visit` 先選校，指定 `/visit/:key` 直接帶入聯絡與安排。第一步下一步前先確認校區設定；第二步常駐所選校區、地址與更換入口。手機第一步用輕量邀請，進入第二步／成功後收成簡短頁名，讓聯絡表單前移。
- 聯絡表單只展開稱呼、手機與同意事項；年齡、接電話時段與提問放在原生可展開選填區。清楚說明接電話時段不是參觀時段。手機輸入至少 16px，主要按鈕至少 52px，校區整列至少 80px。
- 失敗不清空、換校保留內容但清除過期錯誤與時段；必填錯誤顯示於欄位旁並聚焦第一個錯誤。手機號碼可去除複製／自動填入的空格和連字號，API 仍收到 09 開頭十碼；不截斷不合法電話。
- 沿用即時 booking-config resolver，inquiry 才顯示表單；paused／line／phone／external 顯示該校可用方式，slots 仍未開放。不補造分校 LINE，不把義華聯絡方式帶到別校。
- 送出中不能切校、改輸入或重複送出；網路重試保留 idempotency key。沿用 server status 的三種成功語意，inquiry 不宣稱成立；成功後顯示資料摘要及該校聯絡入口，移除會另建案件卻稱作「修改內容」的動作。
- 樣式放在 `visit-booking.css` 並透過 VisitForm scoped 載入，不影響其他頁。根目錄 vanilla 仍凍結；三案 mock-up 留作設計追溯。正式前台原始碼已整合，尚未部署。
- 驗證使用五校 fixture 與合成預約 API；80 項 web 單元測試、Nuxt typecheck、19 組 Chromium 畫面／互動檢查通過。200% 文字放大範圍為預約表單；Safari／iOS 實機尚未驗證。

分校社群以頁首電話對應的已發布校區為準；沒有對應校區就不猜填。四平台依 IG、FB、YouTube、LINE 排列，已提供網址才是可操作連結，未提供者以虛線卡及「待提供」呈現。機構帳號不重標為分校社群；義華現有 FB／LINE 可用，IG／YouTube 待提供。


## 預約 A 的資料需求擴充（2026-09-22 截圖定案）

- 依使用者截圖，保留 A 的選校 → 資料兩步流程。第二步明確分為「參觀安排、孩子資料、家長聯絡方式」，加入孩子姓名／生日、Email 與得知管道。這次裁定取代先前只展開家長稱呼／手機的最小表單版本。
- 日期只列該校公開 API 回傳的可用日期，選日後再選當日場次與剩餘組數；切日期清除場次，換校清除過期時段但保留孩子／家長所有輸入。空場次及讀取失敗提供重試、該校電話，不呈現假時段。預約場次與接電話時段分開。
- 孩子姓名、生日與既有家長稱呼／手機／同意事項必填。Email 與得知管道選填；得知管道為原生 checkbox 多選，含 Facebook、Google 評論、媽媽社團、親友介紹、其他。出生年月日採原生日曆輸入，以台北日期排除未來生日；不再重複要求孩子年齡。
- 桌機第二步的迎賓照片區與較長表單分開；足夠寬高時照片區跟隨捲動，避免拉成整段空白墨綠。手機仍維持單欄，日期場次與資料組別清楚分隔。
- 原先 slots 尚未開放的 UI 限制，依本次日期／場次需求改為校方可設定的模式；不自動替任一校啟用或新增真實場次。後台確認方式預設人工，成功畫面仍以 server status 區分待確認／成立。
- 兒童資料／Email／來源為獨立資料欄位；後台明細與 CSV 可讀，姓名／Email 可搜尋，保存政策一併匿名化。通知與公開家長 schema 不擴增個資。本輪僅本機與隔離測試庫完成，尚未部署。

## 預約頁首頁風格（2026-09-23 定案：C 的第一步＋A 的第二步）

- 使用者要求把 `/visit` 改成首頁風格，三案比稿（A 分校線稿、B 拍立得、C 首頁節奏）後選 **C 的第一步＋A 的第二步**，取代 09-22 預約 A 的外觀（墨綠迎賓區＋框內表單）；A 的流程、欄位、錯誤處理與成功語意規則全部沿用。B 拍立得便條紙落選（最接近 PRODUCT.md「過量裝飾」），不要再套回預約頁。
- 第一步＝首頁「關於常春藤」：薄荷色帶 `#c5ded0`、「預約／參觀」巨大淡字、左文右圖；選校是五張 3:4 圓角照片卡（名字壓在底部漸層上），選中為雙層外環＋打勾。**未選的卡維持原色**（2026-09-23 晚使用者裁定：原本選中後其他卡降飽和＋壓暗，看起來像被停用，已拿掉，不要加回任何變暗／降彩／降透明）；手機改一列一張 112px 橫條。色帶以下用整寬米白承接。
- 第二步與送出後＝首頁「分校資訊」：頁名收成一行，左欄圓角實景＋明體校名（sticky，桌機 ≥961 且高 ≥720），右欄不包框表單；分段用「短線＋小標」，提示用淡色底塊（不用側邊色條），送出鈕金色膠囊。≤960 左欄收成表單上方的一條。送出結果頂端放該校線稿。
- 兩步用 `data-step` 切色票：第一步墨綠 `#203f32`、深綠膠囊；第二步墨藍灰與金色膠囊（同首頁分校資訊）。按鈕顏色因此在兩步不同，屬刻意延續各自來源區塊。
- 實作注意：styles.css 仍有舊全域 `.visit-intro`（sticky top:200px）與 `.visit-page`，新迎賓區叫 `.visit-welcome`，`.visit-page` 的底色、內距、min-height 在 visit-booking.css 明寫。`/visit/manage` 尚未跟著改。

## 示意消息標註（2026-09-24）

消息與活動還沒進後台前，首頁兩個標題的英文 kicker 後面加「示意內容」外框膠囊（`currentColor` 框、11px、700），由 `news.sampleNote` 是否有值控制；搬進後台、`sampleNote` 清空後自動消失。區塊底部的 `hn-sample-note` 小字保留。公開站用語以主選單為準：關於常春藤／孩子的一天／五所校園／最新消息，頁尾與麵包屑跟著用。

## 最新消息與活動進後台（2026-09-24）

消息依日期新到舊、活動只列今天（台北）以後，過期自動下架；清單空的時候顯示一行「目前沒有新的消息／近期活動」（`.hn-empty`，`--hn-muted`、`--fs-sm`），不藏整個區塊，因為消息紙的翻頁動畫和收尾到頁尾的動畫都靠這一段。示意標示只由 `sample_note` 控制，不再寫死在對話框裡。
