# B4 建築融景探索

2026-09-21：依使用者「融入到背景中、補足圖片」的提議，沿用已選 B 左右分景，將五校現有線稿與照片放在同一畫面座標中。照片左緣以 CSS mask 淡入，左側線稿融入米白，聯絡資訊周圍留白；手機改為照片下緣過渡至線稿。兩層隨同一捲動元素切換，不新增獨立裝飾卡。

- 預覽入口：`index.html`，可對照原版 B，切換桌機／手機。
- 滿版：`stage.html?direction=b`；`&fallback=1` 可檢查非原生 scroll timeline 模式。
- 共用上一輪 `campus-fullscreen-20260921/` 的資料、圖示、排版與捲動機制。
- 照片與線稿皆引用現有素材，不改來源圖片；目前為相近構圖的融合示意，並非逐像素對齊的建築重建。
- 保留無句號校名、地圖／電話／社群圖示與地址連結。LINE 未提供的校區保持待補。
- 此輪仍為獨立 mock-up，尚未整合至 Nuxt 首頁。

驗證與截圖：`checks.json`、`shots/`。

Chrome 實測 1440×900、1024×768、390×844、320×740，五校正反向切換共 28 個狀態；素材載入、照片／線稿同步、地址連結、無水平溢出、預約狀態與切校列無重疊通過。鍵盤、JS 動效備援、減少動態、高對比、離開展示段及原版對照通過。`node --check app.js`、新增 JS 語法檢查、`python3 package_preview.py`、`git diff --check` 通過，重打包後 `preview.html` 無差異。
