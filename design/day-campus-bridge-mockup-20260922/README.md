# 孩子的一天 → 分校承接 mock

2026-09-22，依使用者要求製作的獨立方向比較，尚未選定、未整合、未部署。

- A 留白一句話：最後一張拍立得之後，米白底雙行承接，再接既有分校標題。
- B 故事留在畫面裡：桌機與最後一張拍立得並列，手機接在照片下方，保留影片背景。
- C 輕輕接進分校：一句較小的引言放在分校標題上方，縮短過渡篇幅。

開啟 `index.html` 可切 A/B/C 與 390px 手機尺寸；`preview.html?v=a|b|c` 可直接查看。向上可回到最後一張拍立得，向下可看完整分校區。預覽借用現有照片、影片、線稿與字型，不生成校園素材；故事來源註記沿用現行資料。

這是局部版面與閱讀節奏 mock。只重現最後一張拍立得與分校區，不是首頁完整六卡，也未複製正式站的 WebGL 紙張、水滴控制器與負邊距 sticky 揭幕。拍立得可切背面，五校可切換，背景可暫停。校園輪播預設手動，避免比稿時照片自行變動；播放鍵可啟用四秒輪播。預約入口開啟本機 Nuxt 的既有頁面，不送出資料。

`data.js` 由建立當時的公開 fixture 擷取，聯絡資料不另外編造。標題用既有 LINE Seed 700，分校沿用校名專用明體；新承接文字已用 fontTools 檢查字集。

驗證：Chromium 320／390／768／1440px × 三案共 12 個版面無水平溢出、無失敗圖片或執行錯誤；五校切換、鍵盤、比較頁 A/B/C、390px iframe、翻面、回到段落開頭、減少動態通過。證據位於 `output/playwright/day-campus-bridge-20260922/`。本機預覽服務只開放本次 mock 及所需公開素材的明確白名單，其餘路徑 404。
